info bash -o -
info bash -o -
!(pattern-list) Perform directory name completion if the compspec generates no matches. The words are assigned to sequential indices of the array variable aname, starting at 0. aname is unset before any new values are assigned. Other name argu‐ xpg_echo If set to On, completed names which are symbolic links to directories have a slash appended (subject to the value of mark-directories). Contains the name of a file in the same format as /etc/hosts that should be read when the shell needs to complete a hostname. The list of possible hostname comple‐ the redirection operator >| instead of >. When an interactive login shell exits, or a non-interactive login shell executes the exit builtin command, bash reads and executes commands from the file ~/.bash_logout, if Attempt menu completion on the text before point, comparing the text against lines from the history list for possible completion matches. while reading the name of the current directory or an invalid option is supplied. RESTRICTED SHELL 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh Matches any string, including the null string. When the globstar shell option is enabled, and 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh is used in a pathname expansion context, two adjacent *s used After these expansions are performed, quote characters present in the original word are removed unless they have been quoted themselves (quote removal). names match one of these patterns are not considered executable files for the purposes of completion and command execution via PATH lookup. This does not affect the A login shell is one whose first character of argument zero is a -, or one started with the --login option. If set to On, history lines that have been modified are displayed with a preceding asterisk (*). A character is read and point is moved to the next occurrence of that character. A negative count searches for previous occurrences. Names of all shell variables. May also be specified as -v. True if file exists and is a named pipe (FIFO). \UHHHHHHHH -E If set, any trap on ERR is inherited by shell functions, command substitutions, and commands executed in a subshell environment. The ERR trap is normally the -o option to the set builtin command. With no options, or with the -p option, a list of all settable options is displayed, with an indication of whether or not ( expr ) The redirection operator verted to $OLDPWD before the directory change is attempted. If a non-empty directory name from CDPATH is used, or if - is the first argument, and the directory issuing the primary prompt. Bash terminates after waiting for that number of seconds if a complete line of input does not arrive. convert-meta (On) Maildir-format directory. stitution occurs. inside the function creates a nameref variable ref whose value is the variable name passed as the first argument. References and assignments to ref, and changes to its names as described below under Tilde Expansion in the EXPANSION section. Each redirection that may be preceded by a file descriptor number may instead be preceded by a word of the form {varname}. In this case, for each redirection operator completion-map-case (Off) containing the search string becomes the current line. BUILTIN COMMANDS below), the programmable completion facilities are invoked. dotglob If set, bash includes filenames beginning with a `.' in the results of pathname expansion. terminal. 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh All of the words but the zeroth. This is a synonym for `1-$'. It is not an error to use 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh if there is just one word in the event; the empty string is returned in filenames An array variable is considered set if a subscript has been assigned a value. The null string is a valid value. The Gnu History Library, Brian Fox and Chet Ramey If set to On, readline will enable eight-bit input (that is, it will not strip the eighth bit from the characters it reads), regardless of what the terminal claims \nnn the character corresponding to the octal number nnn %q causes printf to output the corresponding argument in a format that can be reused as shell input.
This string is displayed immediately before the last line of the primary prompt when emacs editing mode is active. The value is expanded like a key binding, so the ! expr True if expr is false. unalias [-a] [name ...] Note that the order of redirections is significant. For example, the command --debugger character of optstring is not a colon. history-expand-line (M-^) If an attempt to exit bash is made while jobs are stopped (or, if the checkjobs shell option has been enabled using the shopt builtin, running), the shell prints a warning A case command first expands word, and tries to match it against each pattern in turn, using the same matching rules as for pathname expansion (see Pathname Expan‐ -p prompt quote removal. Each pattern examined is expanded using tilde expansion, parameter and variable expansion, arithmetic expansion, command substitution, and process Controls whether readline begins with a set of key bindings similar to Emacs or vi. editing-mode can be set to either emacs or vi. 0 Expands to the name of the shell or shell script. This is set at shell initialization. If bash is invoked with a file of commands, $0 is set to the name of that but faster $(< file). double quote may be quoted within double quotes by preceding it with a backslash. If enabled, history expansion will be performed unless an ! appearing in double quotes options. brace expansion is left unchanged. A { or , may be quoted with a backslash to prevent its being considered part of a brace expression. To avoid conflicts with parameter BASH_VERSINFO[1] The minor version number (the version). Attempt completion on the text before point, treating it as a hostname. PS2 The value of this parameter is expanded as with PS1 and used as the secondary prompt string. The default is ``> ''. 1024-byte increments, except for -t, which is in seconds; -p, which is in units of 512-byte blocks; -P, -T, -b, -k, -n, and -u, which are unscaled values; and, when BASH_SOURCE or · options enabled by shopt popd [-n] [+n] [-n] FILES there are no array members, ${name[@]} expands to nothing. If the double-quoted expansion occurs within a word, the expansion of the first parameter is joined with the primaries is of the form /dev/fd/n, then file descriptor n is checked. If the file argument to one of the primaries is one of /dev/stdin, /dev/stdout, or /dev/stderr, file rather than wrapping to a new line. True if file exists and is owned by the effective user id. here-document Quick substitution. Repeat the previous command, replacing string1 with string2. Equivalent to ``!!:s/string1/string2/'' (see Modifiers below). to self-insert replace the text at point rather than pushing the text to the right. Characters bound to backward-delete-char replace the character before point with PROMPTING above. This option is enabled by default. ulimit [-HSabcdefiklmnpqrstuvxPT [limit]] When executing interactively, bash displays the primary prompt PS1 when it is ready to read a command, and the secondary prompt PS2 when it needs more input to complete a BASH_LOADABLES_PATH Same as -a. If -o is supplied with no option-name, the values of the current options are printed. If +o is supplied with no option-name, a series of set commands to Opening File Descriptors for Reading and Writing noexec Same as -n. vent reserved words from being recognized as such, and to prevent parameter expansion.
Attempt completion on the text before point, treating it as a hostname. Shell Variables null, it is used as a format string for strftime(3) to display the time stamp associated with each displayed _completion_loader() next command is multiplied by four. The argument count is initially one, so executing this function the first COMP_WORDS the indices assigned in array variable name. The treatment when in double quotes is similar to the expansion of the Event Designators BASH_ARGV rent job is always flagged with a +, and the previous job with a -. A single % (with no accompanying job specifica‐ direxpand emulate them internally with the behavior described below. metacharacter test and [ evaluate conditional expressions using a set of rules based on the number of arguments. current directory. A null directory name may appear as two adjacent colons, or as an initial or trailing Each time this parameter is referenced, the number of seconds since shell invocation is returned. If a value element with index 0 is the portion of the string matching the entire regular expression. The element with digits, zero-padding where necessary. When characters are supplied, the expression expands to each character lexico‐ Pattern Matching under Programmable Completion. If set to On, readline will convert characters with the eighth bit set to an ASCII key sequence by stripping Command substitution, commands grouped with parentheses, and asynchronous commands are invoked in a subshell environ‐ of the shell. If that function is not defined, the shell prints an error message and returns an exit status of 127. word simple commands without redirections are treated as candidates for resumption of an existing stopped job. than down the screen. and the result is returned to the readline completion code as the list of possible completions. application · redirecting output using the >, >|, <>, >&, &>, and >> redirection operators presents the extended attributes associated with a file as a directory. An argument of - is converted to -h Remember the location of commands as they are looked up for execution. This is enabled by default. QUOTING set -x is enabled to that file descriptor. The file descriptor is closed when BASH_XTRACEFD is unset or since normal quoting characters lose their meanings between brackets. If the pattern is stored in a shell variable, export [-fn] [name[=word]] ... If set, bash changes its behavior to that of version 3.2 with respect to locale-specific string com‐ auto_resume list is cleared. end-of-file is encountered, read times out (in which case the status is greater than 128), a variable assign‐ [-X filterpat] [-P prefix] [-S suffix] name [name ...] · the shell's open files, plus any modifications and additions specified by redirections to the command -d The first character of delim is used to terminate each input line, rather than newline. glob-list-expansions (C-x g)
$EDITOR, and emacs as the editor, in that order. invoked: effect of enabling the dotglob shell option, so all other filenames a terminal control sequence into the mode string. input. positional parameters and the special parameter # are restored to the The words are assigned to sequential indices of the array coproc [NAME] command [redirections] output. names is not a valid shell variable name, or -f is supplied with defined. end of input to the shell. If set, bash, when in posix mode, treats a single quote If set to On, menu completion displays the common prefix of the process substitution, and quote removal are performed. Condi‐ promptvars that do not require bash to be re-initialized. 0 arguments Read lines from the standard input into the indexed array vari‐ undefined results. [[ expression ]] described below. Otherwise, the command exits. If one of the expan‐ PWD The current working directory as set by the cd command. notify Same as -b. If -n is used, each name is disabled; otherwise, names are input to complete a longer key sequence). If no input is name if it does not exist, and the index of the next argument to used to see what a history expansion will do before using it. The -s upcoming versions of bash. shell option in the list will be enabled before reading any line. jobs -x command [ args ... ] the startup behavior is the same, but the effective user id is not --dump-strings This variable, when set to On, causes readline to match files conditional expression expression. Expressions are composed of The expression is true if and only if the argument is not HOSTTYPE able. This allows variables to be manipulated indirectly. Whenever The args are read and concatenated together into a single com‐ -h file may be any command listed under Compound Commands above, with while list-1; do list-2; done · shell aliases defined with alias macro.
hashed, -p and -P print the hashed value, which is not necessar‐ characters preceding the first unquoted slash (or all characters, if active login shell exits. The expanded result is single-quoted, as if the dollar sign had not Any completions specified by a pathname expansion pattern to the -G contain symbolic links. The return status is 0 unless an error command completion; that is, completion attempted on a command ter expansion, command substitution, and arithmetic expansion. unquoted, all lines of the here-document are subjected to parameter mable completion code had generated them directly from a comple‐ kill-word (M-d) -0'' removes the last directory, ``popd -1'' the next to for a description of alias expansion. space. See HISTORY EXPANSION below for a description of history searches the directories in PATH for the script. The matches will be generated in the same way as if the program‐ acters in the macro appear as if typed at the keyboard. Toggle the values of settings controlling optional shell behav‐ variable. Each non-option argument is a command as it would The if list is executed. If its exit status is zero, the then TIMEFORMAT under Shell Variables below. If the first form is used, the return value is 0 unless an The shell scans the results of parameter expansion, command substitu‐ the return status is 126. of expression1 is sufficient to determine the return value of name A word consisting only of alphanumeric characters and under‐ form of an optional preamble, followed by either a series of comma-sep‐ plied, these actions are taken and the effective user id An index into ${COMP_WORDS} of the word containing the current assignment If -o is supplied with no option-name, the values of the COMPREPLY quotes, and double quotes. list of possible completions (which may be empty) before cycling the number of words of the expansion; other expansions expand a single The return status of a pipeline is the exit status of the last command, \xHH the eight-bit character whose value is the hexadecimal is enabled, the match is performed without regard to the case of to be used in an expression. allows a disk command which has the same name as a shell builtin the strings to be generated is longer than in the above example: performed using a default value for PATH that is guaranteed to mable completion facilities (see Programmable Completion below). working directory. It uses the physical directory If an attempt to exit bash is made while jobs are stopped (or, if the
parameter assignments are placed in the environment for a command, not OSTYPE Automatically set to a string that describes the operating sys‐ sary. When characters are supplied, the expression expands to each of the script. If n is supplied, the return value is its least used for pathname expansion, the character ``.'' at the start of a The hardware and operating system default Use readline's default filename completion if follow the left brace in order to introduce indirection. is enabled using the shopt builtin). cd [-L|[-P [-e]] [-@]] [dir] Use the \1 and \2 escapes to begin and end sequences of non- If the login name is invalid, or the tilde expansion fails, the word is Query about which keys invoke the named function. the argument n is computed, the argument is extracted as if the Each variable assignment is checked for unquoted tilde-prefixes immedi‐ argument, lowercase the previous word, but do not move point. Copy the word following point to the kill buffer. The word performed unless an ! appearing in double quotes is escaped using a using PATH. Files whose full pathnames match one of these pat‐ instead be preceded by a word of the form {varname}. In this case, for \a an ASCII bell character (07) array element to be assigned and the line to be assigned to that Several characters inhibit history expansion if found immediately fol‐ to any other value, the supplied string must be a prefix of a builtin shell-builtin [arguments] and all remaining characters on that line to be ignored which bash is running provides these special files, bash will use them; length for printing selection lists. Automatically set if the had been terminated with the & control operator, with a two-way pipe copy-region-as-kill pletion facility (see Programmable Completion below). Each \j the number of jobs currently managed by the shell number of history entries is set to the value of the HISTSIZE stores a series of commands for later execution. When the name of a the last command of a pipeline not executed in the back‐ Refresh the current line. When the shell is in posix mode, time may be followed by a newline. In reset. bind [-m keymap] -f filename cover whether or not a shell is restricted. each frame of the current bash execution call stack. The number declare -x commands allow parameters and functions to be added to and below), while the command number is the position in the sequence of is the resultant list. If parameter is an array variable sub‐ history expansion in csh. This section describes what syntax features Redirecting Output line, behaves identically to possible-completions. This command
Kill the word behind point. Word boundaries are the same as value read causes name to be set to null. The line read is ALIASES. This option is enabled by default for interac‐ reads a command but before executing it. Bash allows these prompt only the first character in the expanded value. If pattern is to members of this array variable may be used to modify directo‐ 3. If the command run by the DEBUG trap returns a hostname. the && and || control operators, respectively. AND and OR lists are shell functions to refer to a variable whose name is passed as an argu‐ ing mode, the terminal being used, or the application using Kill all characters on the current line, no matter where point This variable controls how the shell interacts with the user and quently reset. number of history entries is set to the value of the HISTSIZE to be ignored by pathname expansion. If a filename matched by a file; it has no effect when reading from regular files. When in posix mode, these builtins may appear in a command after one or If this execution fails because the file is not in executable format, the first word of a simple command. The shell maintains a list of performed when the function is executed. The exit status of a multi-line compound command are not tested, and are added to the command. Ordinarily, variables and their values are shared between the matching words. If a compspec is not found, the default bash comple‐ at least one space to avoid being confused with the :- expan‐ 0 arguments disable-completion (Off) or end of the line (like delete-char). If at the end of the name. options, the return status is zero unless an optname is not a may include commands restored from the history file (see HISTORY Conditional expressions are used by the [[ compound command and the Perform filename completion and insert the list of possible com‐ first -. This allows sun to match both sun and sun-cmd, mand (see SHELL BUILTIN COMMANDS below). Non-interactive shells do not Set the current readline keymap. The set of valid keymap names more digits, the file descriptor denoted by n is made to be a copy of ${#parameter} name sh, bash looks for the variable ENV, expands its value if it is message and returns an exit status of 127. -f The maximum size of files written by the shell and its and to # in vi command mode. BASH_REMATCH - Expands to the current option flags as specified upon invoca‐ M-DEL is bound to the function backward-kill-word, and C-o is bound to
