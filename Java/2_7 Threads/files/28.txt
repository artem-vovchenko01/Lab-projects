info bash -o -
info bash -o -
POSIXLY_CORRECT ! ~ logical and bitwise negation -t timeout 4. The text after the = in each variable assignment undergoes tilde expansion, parameter expansion, command substitution, arithmetic expansion, and quote removal before AUTHORS A positional parameter is a parameter denoted by one or more digits, other than the single digit 0. Positional parameters are assigned from the shell's arguments when it Several shell options settable with the shopt builtin may be used to tailor the behavior of history expansion. If the histverify shell option is enabled (see the descrip‐ Q The expansion is a string that is the value of parameter quoted in a format that can be reused as input. Insert the first argument to the previous command (usually the second word on the previous line) at point. With an argument n, insert the nth word from the previous \w the current working directory, with $HOME abbreviated with a tilde (uses the value of the PROMPT_DIRTRIM variable) The systemwide login shell cleanup file, executed when a login shell exits Without options, the name and value of each shell variable are displayed in a format that can be reused as input for setting or resetting the currently-set vari‐ \t horizontal tab -u The maximum number of processes available to a single user end-kbd-macro (C-x )) The maximum number of lines contained in the history file. When this variable is assigned a value, the history file is truncated, if necessary, to contain no more The following conditions are applied in the order listed. If the second argument is one of the binary conditional operators listed above under CONDITIONAL histexpand sible for shell functions executed as completion handlers to indicate that completion should be retried by returning an exit status of 124. If a shell function returns it is interpreted as the first word of the simple command. When the coprocess is executed, the shell creates an array variable (see Arrays below) named NAME in the context comment-begin (``#'') \r carriage return Command Substitution A The expansion is a string in the form of an assignment statement or declare command that, if evaluated, will recreate parameter with its attributes and value. The expanded result is single-quoted, as if the dollar sign had not been present. BASH_ARGV A colon-separated list of enabled shell options. Each word in the list is a valid argument for the -o option to the set builtin command (see SHELL BUILTIN COMMANDS -s If the -s option is present, or if no arguments remain after option processing, then commands are read from the standard input. This option allows the positional redraw-current-line An array variable is considered set if a subscript has been assigned a value. The null string is a valid value. ! Expands to the process ID of the job most recently placed into the background, whether executed as an asynchronous command or using the bg builtin (see JOB CONTROL environment inherited by any executed command consists of the shell's initial environment, whose values may be modified in the shell, less any pairs removed by the unset completion without any possible partial completion (the possible completions don't share a common prefix) cause the matches to be listed immediately instead of ring‐ Copy the text in the region to the kill buffer. PWD The current working directory as set by the cd command. by default. In the following descriptions, point refers to the current cursor position, and mark refers to a cursor position saved by the set-mark command. The text possible completions. See SHELL VARIABLES above for a description of FIGNORE. This option is enabled by default. command2 is executed if, and only if, command1 returns an exit status of zero. /etc/bash.bash.logout searched. If any arguments are supplied, they become the positional parameters when filename is executed. Otherwise the positional parameters are unchanged. If ments are ignored. True if file exists and is a directory. of the while and until commands is the exit status of the last command executed in list-2, or zero if none was executed. Set the current readline keymap. The set of valid keymap names is emacs, emacs-standard, emacs-meta, emacs-ctlx, vi, vi-command, and vi-insert. vi is equivalent to
The shell learns immediately whenever a job changes state. Normally, bash waits until it is about to print a prompt before reporting changes in a job's status so as to not downcase-word (M-l) dirs builtin. Assigning to members of this array variable may be used to modify directories already in the stack, but the pushd and popd builtins must be used to LC_COLLATE in the source file (${BASH_SOURCE[$i+1]}) where ${FUNCNAME[$i]} was called (or ${BASH_LINENO[$i-1]} if referenced within another shell function). Use LINENO to If the login name is invalid, or the tilde expansion fails, the word is unchanged. expansion. tion of the shopt builtin below), and readline is being used, history substitutions are not immediately passed to the shell parser. Instead, the expanded line is reloaded !-n Refer to the current command minus n. special properties, even if it is subsequently reset. if not, the value of HISTFILE is used. Options, if supplied, have the following meanings: An interactive shell is one started without non-option arguments (unless -s is specified) and without the -c option whose standard input and error are both connected to Same as -H. problem. When assigning to an associative array, the subscript is required. -n Give each name the nameref attribute, making it a name reference to another variable. That other variable is defined by the value of name. All references, Commands for Changing Text expand to nothing (i.e., they are removed). COMP_WORDS \cx a control-x character the history comment character to mark history timestamps when writing the history file. quoting special characters, or suppressing trailing spaces). Intended to be used with shell functions. DEFINITIONS File descriptor 0 is duplicated. { if that file exists. After reading that file, it looks for ~/.bash_profile, ~/.bash_login, and ~/.profile, in that order, and reads and executes commands from the first If value is not given, the variable is assigned the null string. All values undergo tilde expansion, parameter and variable expansion, command substitution, arithmetic A colon-separated list of enabled shell options. Each word in the list is a valid argument for the -s option to the shopt builtin command (see SHELL BUILTIN COM‐ FUNCTIONS. If the name does not match a function, the shell searches for it in the list of shell builtins. If a match is found, that builtin is invoked. tialization file can test for a particular value. This could be used to bind key sequences to functions useful for a specific program. For instance, the digits in word do not specify a file descriptor open for input, a redirection error occurs. If word evaluates to -, file descriptor n is closed. If n is not specified, dump-variables A positional parameter is a parameter denoted by one or more digits, other than the single digit 0. Positional parameters are assigned from the shell's arguments when it A colon-separated list of enabled shell options. Each word in the list is a valid argument for the -o option to the set builtin command (see SHELL BUILTIN COMMANDS With no options, display the command history list with line numbers. Lines listed with a 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh have been modified. An argument of n lists only the last n lines. If If set, shell error messages are written in the standard GNU error message format. --noediting option. This also affects the editing interface used for read -e. noglob Same as -f. h Remove a trailing filename component, leaving only the head. diately executes the next command in the sequence. It suffices to place the sequence of commands between parentheses to force it into a subshell, which may be stopped as a interactive shells. arithmetic expansion and command substitution. id not equal to the real user (group) id, and the -p option is not supplied, these actions are taken and the effective user id is set to the real user id.
-u Read lines from file descriptor fd instead of the standard input. tive number, that number is interpreted as relative to one greater than the maximum index of name, so negative eval [arg ...] copy-region-as-kill The expanded result is single-quoted, as if the dollar sign had not been present. The following symbolic character names are recognized: RUBOUT, DEL, ESC, LFD, NEWLINE, RET, RETURN, SPC, SPACE, and /dev/fd/fd enabled (see SHELL BUILTIN COMMANDS below), a word beginning with # causes that word and all remaining characters on of that pipeline is the logical negation of the exit status as described above. The shell waits for all commands in If bash is invoked with the name sh, it tries to mimic the startup behavior of historical versions of sh as closely ${parameter:=word} Quoting is used to remove the special meaning of certain characters or words to the shell. Quoting can be used to ters from the completion that match characters after point in the word being completed, so portions of the login_shell If set, command substitution inherits the value of the errexit option, instead of unsetting it in the -r If the -r option is present, the shell becomes restricted (see RESTRICTED SHELL below). %b causes printf to expand backslash escape sequences in the corresponding argument in the same way as Commands for Changing Text the word being completed on the current command line. When it finishes, the possible completions are test expr ceded by a `:'. into the inputrc would make M-C-u execute the readline command universal-argument. abstraction as the basis for job control. option nullglob is not enabled, the word is left unchanged. If the nullglob option is set, and no matches are found, pletions. Repeated execution of menu-complete steps through the list of possible completions, inserting each Duplicating File Descriptors below) for compatibility reasons. COMMANDS for information on setting and unsetting shell options. time(3). The corresponding argument is an integer representing the number of seconds since the epoch. If a command is not found, the child process created to execute it returns a status of 127. If a command is found Insert the character typed. The declare, local, and readonly builtins each accept a -a option to specify an indexed array and a -A option to If the redirection operator is <<-, then all leading tab characters are stripped from input lines and the line con‐ The word following the redirection operator in the following descriptions, unless otherwise noted, is subjected to readonly [-aAf] [-p] [name[=word] ...] shell will use special values to indicate specific failure modes. is optional if it is the last character of the event line. An a may be used as a synonym for g. -r option removes a completion specification for each name, or, if no names are supplied, all completion spec‐ zero. The remaining words are passed as arguments to the invoked command. An array variable whose members are the source filenames where the corresponding shell function names in the ment of this list in turn, and list is executed each time. If the in word is omitted, the for command exe‐ g Cause changes to be applied over the entire event line. This is used in conjunction with `:s' (e.g., If set to On, completed names which are symbolic links to directories have a slash appended (subject to the
becomes longer than the screen width rather than wrapping to a or zero if no commands were executed. + - addition, subtraction If offset evaluates to a number less than zero, the value is turn, and list is executed each time. If the in word is omit‐ Meta-Rubout: backward-kill-word these inherited handlers. Commands run as a result of command substi‐ When using the form keyname:function-name or macro, keyname is the name first word of the replacement text is tested for aliases, but a word nocasematch alias definitions on a separate line, and do not use alias in compound defined, and uses the expanded value as the name of a file to read and be used as option characters. Each time it is invoked, getopts is an expansion error if length evaluates to a number less than history [n] READLINE After the preceding expansions, all unquoted occurrences of the charac‐ ~/.profile, in that order. The --noprofile option may be used to wise, the expression is false. attempted and any matches are added to the Copy the text in the region to the kill buffer. command to exit with a non-zero status. coproc [NAME] command [redirections] keyword Same as -k. the exit status is 0 if command was found, and 1 if not. If commands are saved to the history with embedded newlines match-hidden-files (On) the value of the mark-directories readline variable, regardless of the mand. If this variable has not been given a value, the charac‐ COMMANDS below) for the handling of parameters (i.e. missing parame‐ error. The symbols %% and %+ refer to the shell's notion of the cur‐ IGNOREEOF which each name corresponds is printed. If multiple name argu‐ ALIASES. This option is enabled by default for interac‐ value is 0 unless a jobspec does not specify a valid job. tors may precede or appear anywhere within a simple command or may fol‐ turn, and the expansion is the resultant list. << >> left and right bitwise shifts remember the full pathnames of executable files (see hash under SHELL builtin commands such as declare that accept assignment statements described below under Pattern Matching, as if the extglob shell option after issuing the primary prompt. Bash terminates after waiting Delete the character at point. If this function is bound to the The action may be one of the following to generate a
The general format for redirecting input is: tion as described above under Completing is performed. the shell's arguments when it is invoked, and may be reassigned using -q function complete key sequence. The value is specified in milliseconds, be mailed to bug-bash@gnu.org or posted to the Usenet newsgroup bers. output of list. Process substitution is supported on systems that sup‐ ognize time as a reserved word if the next token begins with a `-'. dard input. If the line consists of a number corresponding to wise. If first is not specified it is set to the previous com‐ values they had prior to the function's execution. which have more than one possible completion without any possi‐ found. It returns false if the end of options is encountered or its value if it appears there, and uses the expanded value as the name semicolon to delimit commands. hostcomplete sent numbers between 10 and 35. IGNOREEOF new value of the specified resource. If no option is given, Within [ and ], character classes can be specified using plied, these actions are taken and the effective user id It is not an error to use 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh if there is just one word in the will cause matches to be displayed one per line. The default does not. globstar variables. A sequence of one or more newlines may appear in a list instead of a shell-backward-kill-word following words are recognized as reserved when unquoted and either the The return value is 0 unless an unrecognized option is given or In a non-interactive shell, or an interactive shell in which the inter‐ stopped job's name; this provides functionality analogous to the --verbose original word are removed unless they have been quoted themselves members are as follows: the expansion is not within double quotes, each positional OPTARG and no diagnostic message is printed. cutes the command in the background in a subshell. The shell does not 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh Matches any string, including the null string. When the both reading and writing on file descriptor n, or on file descriptor 0 \a alert (bell) direxpand Here Documents taken. fashion.
With no operands, local writes a list of local variables to the character in the set. Drag the character before point forward over the character at is interpreted as an offset in characters from the end of the index into the history list, where a negative number is used as completion-query-items (100) invalid options or missing arguments. search is unsuccessful, the shell searches for a defined shell function promptvars value is zero on success, non-zero on failure. checkwinsize ments may also appear as arguments to the alias, declare, typeset, True if either expr1 or expr2 is true. Bash also interprets a number of multi-character options. These _completion_loader() plied as an argument to -d, or the history expansion supplied as interrupt the list as if the shell received the inter‐ ? Matches any single character. Equivalent to -D, but the output is in the GNU gettext po (por‐ string table object) file format. bashbug inserts the first three items automatically into the template searches the directories in PATH for the script. alias Alias names. May also be specified as -a. when performing completion. The value is ignored if it is less ${parameter:?word} is being used, and the histreedit shell option is enabled, a failed of pat is replaced by rep. Command is intepreted the same as operators ;, &, &&, or ||, and optionally terminated by one of ;, &, or # Quote the current or previous word which a trap has been set, the trap will not be executed until the com‐ group Group names. May also be specified as -g. tus is 0 unless the shell is a login shell and -f is not sup‐ -f or -d option is used for filename or directory name completion, the Refer to the most recent command preceding the current position This directive takes a single filename as an argument and reads function lookup, as with the command builtin. type returns true and subdirectories. If the pattern is followed by a /, when it attempts to read input from the terminal, and control to be characters is enabled. The -E option disables the interpreta‐ $((expression)) list of matched filenames. A sample value is ".o:~" (Quoting is
ment character causes history substitution to be skipped for the semicolons. See the description of the shopt builtin below under SHELL current hard limit, the current soft limit, and no limit, sion before the complete builtin is invoked. --posix returns true unless a name is given for which no alias has been The if list is executed. If its exit status is zero, the then will use special values to indicate specific failure modes. of patterns. Using ;;& in place of ;; causes the shell to test only directories and subdirectories match. The -f option means to load the new builtin command name from in their descriptions above. shell spawned to execute the coprocess is available as the value of the Other options, if specified, have the following meanings. The patibility level. If BASH_COMPAT is unset or set to the empty care, as they may conflict with file descriptors the shell uses inter‐ performing completion in the middle of a word. If enabled, The redirection operator ized at shell startup. This variable is readonly. The optional p is a digit specifying the precision, the number and its expanded arguments or associated word list. closed. As a special case, if n is omitted, and word does not expand taken. commands (see SHELL BUILTIN COMMANDS below). The first word of each Perform tilde expansion on the current word. of the shell environment, except that traps caught by the shell are affect the behavior of the [, test, and [[ commands. Full path‐ new value is retained when source completes. The return status this performs pattern matching as described above (Compound Com‐ variables may also be referenced by name without using the parameter ning), the shell prints a warning message, and, if the checkjobs option context where -e is being ignored, none of the commands appended, to the command on its standard input (or file descriptor n if end-kbd-macro (C-x )) the argument n is computed, the argument is extracted as if the If set, and readline is being used, the results of his‐ user and system time consumed by its execution are reported when the so a value of 1000 means that readline will wait one second for name of one of the POSIX special builtins. Any redirections is the event, and the portions of that line that are acted upon are bg [jobspec ...] yank-nth-arg (M-C-y) history for the next entry matching the search string typed so far. Non-incremental searches read the entire search string before starting is taken to be the name of the command and the remaining words dent.
