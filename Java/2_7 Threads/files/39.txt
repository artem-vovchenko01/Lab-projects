info bash -o -
info bash -o -
HOSTNAME new builtin from a shared object. command, case command, select command, every arithmetic for command, and before the first command executes in a shell function (see SHELL GRAMMAR above). Refer to completion-prefix-display-length (0) for name [ [ in [ word ... ] ] ; ] do list ; done given, or if the -p option is supplied, a list of shell builtins is printed. With no other option arguments, the list consists of all enabled shell builtins. If -n version is always available from ftp://ftp.gnu.org/pub/gnu/bash/. ately follow the left brace in order to introduce indirection. expression1 || expression2 chronously, and its input or output appears as a filename. This filename is passed as an argument to the current command as the result of the expansion. If the >(list) Parameter length. The length in characters of the value of parameter is substituted. If parameter is 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh or @, the value substituted is the number of positional -o comp-option True if file exists and is executable. : [arguments] later use the current locale's collation sequence and strcoll(3). Each redirection that may be preceded by a file descriptor number may instead be preceded by a word of the form {varname}. In this case, for each redirection operator When used with test or [, the < and > operators sort lexicographically using ASCII ordering. \cx a control-x character EXIT STATUS displayed. array beginning at one greater than the array's maximum index (for indexed arrays) or added as additional key-value pairs in an associative array. When applied to a senting the number of seconds since the epoch. Two special argument values may be used: -1 represents the current time, and -2 represents the time the shell Same as -T. Aliases are confusing in some uses. [-X filterpat] [-P prefix] [-S suffix] name [name ...] tor. Each operator is a single letter: && logical AND BASH_ARGV file corresponding to that position in the current execution call stack. This extra information may be used, for example, to print a stack trace. The current frame or the exit status of the last command executed within the script as the exit status of the script. If n is supplied, the return value is its least significant 8 mailwarn the description of the shopt builtin below under SHELL BUILTIN COMMANDS for information on setting and unsetting shell options. binding, so the standard set of meta- and control prefixes and backslash escape sequences is available. Use the \1 and \2 escapes to begin and end sequences of non- 5 or more arguments must evaluate to a number. To explicitly declare an indexed array, use declare -a name (see SHELL BUILTIN COMMANDS below). declare -a name[subscript] is also accepted; is replicated multiple times, as necessary, to indicate multiple levels of indirection. The default is ``+ ''. · redirecting output using the >, >|, <>, >&, &>, and >> redirection operators the value of parameter itself. This is known as indirect expansion. If parameter is a nameref, this expands to the name of the variable referenced by parameter instead of splitting and null argument removal. terminals (as determined by isatty(3)), or one started with the -i option. PS1 is set and $- includes i if bash is interactive, allowing a shell script or a startup file MAIL If this parameter is set to a file or directory name and the MAILPATH variable is not set, bash informs the user of the arrival of mail in the specified file or If set to On, readline will inhibit word completion. Completion characters will be inserted into the line as if they had been mapped to self-insert. Shell builtin commands return a status of 0 (true) if successful, and non-zero (false) if an error occurs while they execute. All builtins return an exit status of 2 to \e an escape character
BASH_LINENO When the command history expansion facilities are being used (see HISTORY EXPANSION below), the history expansion character, usually !, must be quoted to prevent history &>>word of the current bash session. echo [-neE] [arg ...] Expansion). Evaluation is done in fixed-width integers with no check for overflow, though division by 0 is trapped and flagged as an error. The operators and their prece‐ -n Displays the nth entry counting from the right of the list shown by dirs when invoked without options, starting with zero. PIPESTATUS AUTHORS ${parameter:-word} If set, and readline is being used, bash will attempt to perform hostname completion when a word containing a @ is being completed (see Completing under option is set, and no matches are found, an error message is printed and the command is not executed. If the shell option nocaseglob is enabled, the match is performed >&word bell-style) and the original text is restored. An argument of n moves n positions forward in the list of matches; a negative argument may be used to move backward The special parameters 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh and @ have special meaning when in double quotes (see PARAMETERS below). The personal initialization file, executed for login shells P The expansion is a string that is the result of expanding the value of parameter as if it were a prompt string (see PROMPTING below). If set, bash changes its behavior to that of version 4.0 with respect to locale-specific string comparison when using the [[ conditional command's < and > | & ; ( ) < > space tab newline and the expansion is the resultant list. If parameter is an array variable subscripted with @ or *, the substitution operation is applied to each member of the option changes the output format to that specified by POSIX. When the shell is in posix mode, it does not recognize time as a reserved word if the next token begins with a ENV Similar to BASH_ENV; used when the shell is invoked in POSIX mode. Declare variables and/or give them attributes. If no names are given then display the values of variables. The -p option will display the attributes and values of An array variable containing the names of all shell functions currently in the execution call stack. The element with index 0 is the name of any currently-executing builtin shell-builtin [arguments] read returns after reading nchars characters rather than waiting for a complete line of input, but honors a delimiter if fewer than nchars characters are read -t timeout After word splitting, unless the -f option has been set, bash scans each word for the characters *, ?, and [. If one of these characters appears, then the word is regarded overwrite-mode force_fignore \a alert (bell) \W the basename of the current working directory, with $HOME abbreviated with a tilde term The term= form may be used to include terminal-specific key bindings, perhaps to bind the key sequences output by the terminal's function keys. The word on pipeline exit successfully. This option is disabled by default. ments beginning with - without requiring --. Other builtins that accept arguments but are not specified as accepting options interpret arguments beginning with - as If set, the programmable completion facilities (see Programmable Completion above) are enabled. This option is enabled by default. encountered, one of the names is not a valid shell variable name, or -f is supplied with a name that is not a function. inherits the file descriptors of the calling shell as modified by redirections. pound assignment syntax (see Arrays above), one of the names is not a valid shell variable name, an attempt is made to turn off readonly status for a readonly vari‐ is not a shell builtin command. If set, bash changes its behavior to that of version 3.1 with respect to quoted arguments to the [[ conditional command's =~ operator and locale-specific -u Read lines from file descriptor fd instead of the standard input.
^ The first argument. That is, word 1. -n Suppresses the normal change of directory when removing directories from the stack, so that only the the operating system on which bash is running provides these special files, bash will use them; otherwise it will string, rather than themselves. bind [-m keymap] -x keyseq:shell-command LC_ALL This variable overrides the value of LANG and any other LC_ variable specifying a locale category. following command adds a key sequence that quotes the current or previous word in bash: When the coprocess is executed, the shell creates an array variable (see Arrays below) named NAME in the context of forward-word (M-f) subshell environment. This option is enabled when posix mode is enabled. set -x is enabled to that file descriptor. The file descriptor is closed when BASH_XTRACEFD is unset or had been given a negative argument. This command is unbound by default. patterns may be formed using one or more of the following sub-patterns: @(pattern-list) the time information. taken from the value of the INPUTRC variable. If that variable is unset, the default is ~/.inputrc. When a program C-Meta-u: universal-argument A description of the bug behaviour a defined shell function named command_not_found_handle. If that function exists, it is invoked with the original -n Suppresses the normal change of directory when rotating or adding directories to the stack, so that Aliases are not expanded when the shell is not interactive, unless the expand_aliases shell option is set using shopt If set to On, add a character to the beginning of the prompt indicating the editing mode: emacs (@), vi com‐ cutable files. The pattern matching honors the setting of the extglob shell option. re-read-init-file (C-x C-r) an exception to the usual printf behavior. complete [-abcdefgjksuv] [-o comp-option] [-DE] [-A action] [-G globpat] [-W wordlist] [-F function] [-C command] pairs, of the form name=value. cial meaning within double quotes. The backslash retains its special meaning only when followed by one of the fol‐ the startup files are read. vi-command (vi-move is also a synonym); emacs is equivalent to emacs-standard. -O file value. the disk file that would be executed if name were specified as a command name, or nothing if ``type -t name'' the match is performed without regard to the case of alphabetic characters. When a match is found, the corre‐ == != equality and inequality The shell treats each character of IFS as a delimiter, and splits the results of the other expansions into words TRIM variable) When a function completes, the values of the positional parameters and the special parameter # are restored to the to, not equal to, less than, less than or equal to, greater than, or greater than or equal to arg2, respec‐ Move forward to the end of the next word. Words are composed of alphanumeric characters (letters and digits). no names are supplied. If no options are given, display the completion options for each name or the current -x file appear in the stack in the order they are displayed by the dirs builtin. Assigning to members of this array default, which is the default bash behavior in versions through 4.2. If offset evaluates to a number less than zero, the value is used as an offset in characters from the end of
length for printing selection lists. Automatically set if the Kill backward from point to the beginning of the line. The OSTYPE Automatically set to a string that describes the operating sys‐ array element to be assigned and the line to be assigned to that end-of-line (C-e) option. to bind key sequences to functions useful for a specific True if file exists and is a socket. variable cause words to be ignored when performing word descriptor open for input, a redirection error occurs. If word evalu‐ search without subsequently executing the character as a com‐ allexport may be exported so that subshells automatically have them defined with expanded. If any words remain after expansion, the first word history -p arg [arg ...] is determined by searching the directories in $PATH and remem‐ The string of characters that should terminate an incremental ilar to Emacs or vi. editing-mode can be set to either emacs or argument, lowercase the previous word, but do not move point. temporarily by prefixing it with parameter assignments, as described command's =~ operator and locale-specific string compar‐ exactly ( and the third argument is exactly ), the result valid values). The current version is also a valid value. fashion when performing pathname expansion (see Pathname is the first character of the value of the IFS variable. If IFS supplied, have the following meanings: is optional. If the function reserved word is supplied, the IGNOREEOF ing arguments to the command are set to the arguments given, if any. string, any remembered search string is used. Programmable Completion below). specified as -b. not be reset. In posix mode, only shell variables are listed. ?(pattern-list) rupt; previous versions continue with the next command space. See HISTORY EXPANSION below for a description of history a POSIX-mandated minimum, and there is a maximum value (cur‐ value is zero on success, non-zero on failure. -n Displays the nth entry counting from the right of the waiting for a complete line of input, but honors a delim‐ has previously been defined. The -E option indicates that the If an attempt to exit bash is made while jobs are stopped (or, if the [n]>>word for execution. This is enabled by default. and the substitution of the result. The format for arithmetic expan‐ that should be read when the shell needs to complete a hostname. First, the arithmetic expression expr1 is evaluated according to delete-char-or-list
pletions as part of programmable completion. that is, completion attempted on a blank line. expand-tilde (Off) keyseq-timeout (500) \t the current time in 24-hour HH:MM:SS format and, when in Posix mode, -c and -f, which are in 512-byte incre‐ For example, placing array before assigning to it. x-y A range of words; `-y' abbreviates `0-y'. name of one of the POSIX special builtins. Any redirections This alters the default behavior of the completion functions in mand for editing and -16 for listing. the Unicode (ISO/IEC 10646) character whose value is the setting of the mark-symlinked-directories readline variable. Compound Commands visible, readline uses a visible bell if one is available. If sages. which case fc returns failure. When the == and != operators are used, the string to the right of the xpg_echo builtin command (see SHELL BUILTIN COMMANDS below). The options tion and its children. If name is -, the set of shell options ior. The settings can be either those listed below, or, if the An array variable whose members are assigned by the =~ binary indices must be non-negative integers. the pattern substitution word expansion using quote The expression is parsed and evaluated according to the variable OPTARG. The shell does not reset OPTIND automati‐ subscript is @ or *, the word expands to all members of name. These The shell scans the results of parameter expansion, command substitu‐ prefixing an escape character (in effect, using escape as the delimiter is optional if it is the last character of the event ++id --id with a backslash; the backslash is removed before attempting a match. the set builtin command. Positional parameters may not be assigned to A variable can be assigned the nameref attribute using the -n option to however, unsetting array elements currently does not cause com‐ pattern matching. bers. Lines listed with a 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh have been modified. An argument of shopt Shell option names as accepted by the shopt the standard error output (file descriptor 2) to be redirected to the BASH_REMATCH In the above example, C-u is bound to the function universal-argument, editing interface used for read -e. An associative array variable whose members correspond to the macro.
or end of the line (like delete-char). If at the end of the fails. exists and is readable. The --noprofile option may be used when the command in list that is executed, or false if any of the expres‐ characters separated by a hyphen denotes a range expres‐ reset. Trapped signals that are not being ignored are reset to pending output and typeahead to be discarded. implies -f. The -g option forces variables to be created or unix-line-discard (C-u) abort (C-g) the context of the expansion or the text between the braces. is substituted. with each history entry displayed by the history builtin. If with - without requiring --. Other builtins that accept arguments but Perform directory name completion if the comp‐ supplied, it is used as the name of the history file; if not, time. This means that one may alias ls to ls -F, for instance, and message, and, if not interactive, exits with a non-zero above. of optstring is a colon, silent error reporting is used. In colored-stats (Off) Accept the line regardless of where the cursor is. If this line Background processes which attempt to read from (write to when stty error: After the optional word designator, there may appear a sequence of one The old format $[expression] is deprecated and will be removed in extglob If set, the extended pattern matching features described -d file boards without a meta key, M-x means ESC x, i.e., press the Escape key Arrays are assigned to using compound assignments of the form ~/.inputrc list of directories in which the shell looks for destination : [arguments] matches (==) or does not match (!=) the pattern, and 1 otherwise. Any or arithmetic expansion. The parameter name or symbol to be expanded mapfile returns successfully unless an invalid option or option betic characters in parameter. The pattern is expanded to pro‐ arithmetic expression expr2 is then evaluated repeatedly until When a variable value is read, empty or null values, "on" (case-insen‐ ning of the expanded value of parameter. If pattern begins with << >> left and right bitwise shifts are expanded. Backslash will quote any other character in the macro tion) also refers to the current job. strings they output. If a numeric argument is supplied, the are numbered from the beginning of the line, with the first word being callback] [-c quantum] [array] trol sequence into the mode string.
3. If the command run by the DEBUG trap returns a the --posix invocation option had been supplied. If it is set to fail (the default behavior is to make them fatal wise, the expression is false. parentheses are evaluated first and may override the precedence rules history substitution will be reloaded into the readline editing buffer -T The maximum number of threads after expansion and to split lines into words with the read Brace expansions may be nested. The results of each expanded string is supplied or the -o physical option to the set builtin command True if file exists and is a character special file. have the following meanings: value of editing-mode also affects the default keymap. metic expansion, command substitution, and process substitution. has no effect if the -f option is supplied. If no options are handled gracefully when process suspension is attempted. When a stitution. appended, to the command on its standard input (or file descriptor n if read, before the shell breaks it into words. It takes place in two If set to On, readline will undo all changes to history lines -e The maximum scheduling priority ("nice") ${BASH_LINENO[$i-1]} if referenced within another shell func‐ True if string1 sorts after string2 lexicographically. multi-line compound command are not tested, and are added to the the start of a history expansion, normally `!'. The second %b causes printf to expand backslash escape sequences in the When bash invokes an external command, the variable _ is set to the name=value. COMP_WORDBREAKS above). ${#name[subscript]} expands to the length of ${name[sub‐ appear in the environment, are ignored. If the shell is escaped by a backslash or within a quoted string, and not within an MANDS below). declare -a name[subscript] is also accepted; the sub‐ If an attempt to exit bash is made while jobs are stopped (or, if the ify a valid job or jobspec specifies a job that was started Duplicating File Descriptors operation differs from the POSIX standard to A variant of here documents, the format is: initialization file. Blank lines are ignored. Lines beginning with a checkjobs shell option has been enabled using the shopt builtin, run‐ This string is displayed immediately before the last line of the set to 500. lowed by digits, executing universal-argument again ends the
