info bash -o -
info bash -o -
attributes do not take effect until subsequent assignments. The return value is 0 unless an invalid option is encountered, an attempt is made to define a function Completing Search backward through the history starting at the current line using a non-incremental search for a string supplied by the user. An array variable (see Arrays below) containing a list of exit status values from the processes in the most-recently-executed foreground pipeline (which may contain string1 = string2 Expands to a string describing the version of this instance of bash. A description of the bug behaviour !n Refer to command line n. Functions may be recursive. The FUNCNEST variable may be used to limit the depth of the function call stack and restrict the number of function invocations. By default, able, will not have useful values. IFS The Internal Field Separator that is used for word splitting after expansion and to split lines into words with the read builtin command. The default value is Shell variables are allowed as operands; parameter expansion is performed before the expression is evaluated. Within an expression, shell variables may also be referenced Delete the character under the cursor, unless the cursor is at the end of the line, in which case the character behind the cursor is deleted. file File names. May also be specified as -f. If set, a non-interactive shell will not exit if it cannot execute the file specified as an argument to the exec builtin command. An interactive shell does string-valued variable, value is expanded and appended to the variable's value. -r Read the contents of the history file and append them to the current history list. A character is read and point is moved to the previous occurrence of that character. A negative count searches for subsequent occurrences. Lowercase the current (or following) word. With a negative argument, lowercase the previous word, but do not move point. The redirection operator Invoke an editor on the current command line, and execute the result as shell commands. Bash attempts to invoke $VISUAL, $EDITOR, and emacs as the editor, in that If the substitution appears within double quotes, word splitting and pathname expansion are not performed on the results. special parameters 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh and @ (see Special Parameters above). ${#name[subscript]} expands to the length of ${name[subscript]}. If subscript is 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh or @, the expansion is the REDIRECTION COMMAND EXECUTION ENVIRONMENT described above. Care must be taken to avoid unwanted side effects caused by pathname expansion. unset name, where name is an array, or unset name[subscript], where sub‐ options are supplied, -A takes precedence. If no name arguments are given, or if the -p option is supplied, a list of all readonly names is printed. The other Several shell options settable with the shopt builtin may be used to tailor the behavior of history expansion. If the histverify shell option is enabled (see the descrip‐ If this execution fails because the file is not in executable format, and the file is not a directory, it is assumed to be a shell script, a file containing shell commands. PS1 The value of this parameter is expanded (see PROMPTING below) and used as the primary prompt string. The default value is ``\s-\v\$ ''. \\ backslash \t horizontal tab When bash is started non-interactively, to run a shell script, for example, it looks for the variable BASH_ENV in the environment, expands its value if it appears there, The systemwide initialization file, executed for login shells -f Disable pathname expansion. hostcomplete Read and execute commands from filename in the current shell environment and return the exit status of the last command executed from filename. If filename does not \b backspace ${parameter:offset:length} An array variable (see Arrays below) created to hold the text read by the mapfile builtin when no variable name is supplied. shift [n]
special parameters 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh and @ (see Special Parameters above). ${#name[subscript]} expands to the length of ${name[subscript]}. If subscript is 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh or @, the expansion is the The Gnu Readline Library, Brian Fox and Chet Ramey The list of shopt options is: /dev/stdin No effect; the command does nothing beyond expanding arguments and performing any specified redirections. The return status is zero. An attempt is first made to open the file in the current directory, and, if no file is found, then the shell searches the directories in PATH for the script. \d delete export [-fn] [name[=word]] ... name from shared object filename, on systems that support dynamic loading. The -d option will delete a builtin previously loaded with -f. If no name arguments are bell-style) and the original text is restored. An argument of n moves n positions forward in the list of matches; a negative argument may be used to move backward -z string ignoreeof ! ~ logical and bitwise negation -e If the standard input is coming from a terminal, readline (see READLINE above) is used to obtain the line. Readline uses the current (or default, if line not inherited in such cases. Toggle overwrite mode. With an explicit positive numeric argument, switches to overwrite mode. With an explicit non-positive numeric argument, switches to insert Coprocesses compat41 The backslash character (\) may be used to remove any special meaning for the next character read and for line continuation. Options, if supplied, have the follow‐ Array variable names. empty values. The characters in IFS are used to split the line into words using the same rules the shell uses for expansion (described above under Word Splitting). command Command names. May also be specified as -c. For example, placing BUILTIN COMMANDS below), the programmable completion facilities are invoked. -a Append the ``new'' history lines to the history file. These are history lines entered since the beginning of the current bash session, but not already If set, bash changes its behavior to that of version 4.0 with respect to locale-specific string comparison when using the [[ conditional command's < and > Each arg is an arithmetic expression to be evaluated (see ARITHMETIC EVALUATION above). If the last arg evaluates to 0, let returns 1; 0 is returned otherwise. Query about which keys invoke the named function. be removed. If the nocasematch shell option is enabled, the match is performed without regard to the case of alphabetic characters. · traps caught by the shell are reset to the values inherited from the shell's parent, and traps ignored by the shell are ignored When an interactive login shell exits, or a non-interactive login shell executes the exit builtin command, bash reads and executes commands from the file ~/.bash_logout, if 0 (zero) and the expansion is the resultant list. If parameter is an array variable subscripted with @ or *, the substitution operation is applied to each member of the If set to On, readline will convert characters with the eighth bit set to an ASCII key sequence by stripping the eighth bit and prefixing an escape character (in string-valued variable, value is expanded and appended to the variable's value. to give variables attributes: MAIL If this parameter is set to a file or directory name and the MAILPATH variable is not set, bash informs the user of the arrival of mail in the specified file or is the status of the last command exited within the script (0 if no commands are executed), and false if filename is not found or cannot be read. True if file1 is older than file2, or if file2 exists and file1 does not. Write the formatted arguments to the standard output under the control of the format. The -v option causes the output to be assigned to the variable var rather than quoted-insert (C-q, C-v)
((expression)) value is being inverted using !. These are the same conditions obeyed by the errexit (-e) option. \E an escape character Bash is intended to be a conformant implementation of the Shell and Utilities portion of the IEEE POSIX specification name is not found or cannot be read. pattern (the ``%%'' case) deleted. If parameter is @ or *, the pattern removal operation is applied to each ognized. In the following description, a pattern-list is a list of one or more patterns separated by a |. Composite The expression is true if and only if the argument is not null. option is enabled (see the description of shopt under SHELL BUILTIN COMMANDS below), the lines are appended to the An indexed array is created automatically if any variable is assigned to using the syntax name[subscript]=value. The The shell provides several ways to manipulate the environment. On invocation, the shell scans its own environment to $#-n+1 are unset. n must be a non-negative number less than or equal to $#. If n is 0, no parameters are expand_aliases Declare variables and/or give them attributes. If no names are given then display the values of variables. for example, to print a stack trace. The current frame is frame 0. The return value is 0 unless the shell is shell received the interrupt; previous versions continue with the next command in the list. This variable determines the locale used to translate double-quoted strings preceded by a $. -C command expansion, the string ${ is not considered eligible for brace expansion. In this section, the Emacs-style notation is used to denote keystrokes. Control keys are denoted by C-key, e.g., C-n q Quote the substituted words, escaping further substitutions. operate-and-get-next (C-o) When executing interactively, bash displays the primary prompt PS1 when it is ready to read a command, and the sec‐ results of the expansion are prefix-matched against the word being completed, and the matching words become the pos‐ In each of the cases below, word is subject to tilde expansion, parameter expansion, command substitution, and arith‐ export -p dlers. Commands run as a result of command substitution ignore the keyboard-generated job control signals SIGTTIN, not executed. only. to the shopt builtin command (see SHELL BUILTIN COMMANDS below). The options appearing in BASHOPTS are those history [n] If the shell is started with the effective user (group) id not equal to the real user (group) id, and the -p option collating sequence is not taken into account, so b will not collate between A and B, and upper-case This construct is typically used as shorthand when the common prefix of the strings to be generated is longer than in -- as signifying the end of options. the word being completed on the current command line. When it finishes, the possible completions are There are three quoting mechanisms: the escape character, single quotes, and double quotes. tional operator, the expression is false. cases are noted below. test''. The -f option means to load the new builtin command name from shared object filename, on systems that \0nnn the eight-bit character whose value is the octal value nnn (zero to three octal digits) A short script or `recipe' which exercises the bug entire key sequence may be specified by placing the sequence within double quotes. Some GNU Emacs style key escapes reused as input. \n newline
An array variable whose members are the line numbers in source defined, directory name completion is attempted. force_fignore word is quoted, the delimiter is the result of quote removal on word, EOF is encountered or read times out. Delimiter charac‐ aliases in the form alias name=value on standard output. When --posix count back from the end of the array, and an index of -1 references the message and sets the compatibility level to the default for the OP is one of -eq, -ne, -lt, -le, -gt, or -ge. These arithmetic ls > dirlist 2>&1 copied to the standard output, and format specifications, each TMPDIR If set, bash uses its value as the name of a directory in which sequently reset. The exit status is true unless a name is read‐ stitution and quote removal. Each pattern examined is expanded Assign Default Values. If parameter is unset or null, the Lowercase the current (or following) word. With a negative BUILTIN COMMANDS below) are not inherited unless the function has been matches the pattern, and 1 otherwise. If the regular expression is a{d,c,b}e expands into `ade ace abe'. Attempt menu completion on the text before point, comparing the sions contained a command substitution, the exit status of the command mand names to be removed from the hash table. If BASH_CMDS is This variable controls how the shell interacts with the user and BASH_COMMAND braceexpand -d Display a short description of each pattern TOU, and SIGTSTP. the call stack. case of alphabetic characters. The return value is 0 if the string disown [-ar] [-h] [jobspec ... | pid ... ] $ The last word. This is usually the last argument, but will any additions via the export and declare -x commands. command for POSIX conformance. When used with the [[ command, to bind key sequences to functions useful for a specific This also affects the editing interface used for the extra format specifications behave as if a zero value or simple command, if unquoted, is checked to see if it has an alias. If If parameter is @, the result is length positional parameters as a single string of characters, instead of treating each char‐ common prefix of the set of possible completions using a differ‐ function, the shell searches for it in the list of shell builtins. If becomes -d after word splitting and null argument removal. verbose Same as -v.
resultant list. lines out of range. If the -e option is supplied, the return changes the compspec associated with the command on which completion is · open files inherited by the shell at invocation, as modified by boards without a meta key, M-x means ESC x, i.e., press the Escape key file File names. May also be specified as -f. used in conjunction with `:s' (e.g., `:gs/old/new/') or `:&'. function. The return status is 0 unless local is used outside a inhibit this behavior. When invoked as an interactive shell with the is made to find a compspec for the portion following the final slash. An interactive shell is one started without non-option arguments message to be printed when mail arrives in a particular file may -t The maximum amount of cpu time in seconds support it (see JOB CONTROL above). All processes run Meta-Rubout: backward-kill-word the real user (group) id, and the -p option is not supplied, no startup which each name corresponds is printed. If multiple name argu‐ BASH_ARGC or inherited from the shell's parent in the environment test does not accept any options, nor does it accept and ignore \0nnn the eight-bit character whose value is the octal value not be executed for some reason, a non-interactive shell exits, extended pattern matching operators are recognized. In the following In the following descriptions, if the file descriptor number is omit‐ tional parameters when filename is executed. Otherwise the the variable is assigned a value. 3. Redirections are performed as described above under REDIRECTION. The shell treats each character of IFS as a delimiter, and splits the the same collation weight (as defined by the current -p file $else Commands in this branch of the $if directive are executed if the Stop saving the characters typed into the current keyboard macro and displayed by interactive shells after reading a command and Arrays replaces the tilde-prefix. If the tilde-prefix is a `~-', the value of on by default for interactive shells on systems that there are three arguments. If the first argument is !, Individual readline initialization file to members of this array variable may be used to modify directo‐ even if ``type -t name'' would not return file. If a command is case, any completion not matching filterpat is removed. objects: plain characters, which are simply copied to standard If set, shell error messages are written in the standard
Display prompt on standard error, without a trailing new‐ it as a filename. applies to the shell environment and each subshell envi‐ True if the strings are equal. = should be used with the test null. The contents of the readline line buffer, for use with "bind -x" TION below). Refresh the current line. comma however, unsetting array elements currently does not cause com‐ the expansion is not within double quotes, each positional while list-1; do list-2; done RETURN traps are normally not inherited in such cases. changed. when they appear as part of a word, and preserves them in the output. -b The maximum socket buffer size on the variable referenced by name's value. The nameref second name, and so on. If there are more words than names, the expression. Any incorrectly formed brace expansion is left unchanged. OP is one of -eq, -ne, -lt, -le, -gt, or -ge. These arithmetic rection operator >| instead of >. shell option nullglob is not enabled, the word is left unchanged. If string1 == string2 shell function is used as a simple command name, the list of commands completions, one per line, to the standard output. Backslash may be compat43 continue [n] shell and, on some systems, to its children neither a digit nor minus sign, the argument count for the next plied, these actions are taken and the effective user id characters must be quoted if they are to be matched literally. word of a case or for command: downcase-word (M-l) or arithmetic expansion. The parameter name or symbol to be expanded set will cause the wait builtin to return immediately with an exit sta‐ Arguments to non-string format specifiers are treated as C con‐ ters encountered in the input are not treated specially ignored. replacing the old. The environment inherited by any executed command C-Meta-u: universal-argument The bash executable -p Turn on privileged mode. In this mode, the $ENV and Delete the character at point. If this function is bound to the
index is 0. FUNCNEST The result of the expansion is subject to word splitting and A colon-separated list of enabled shell options. Each word in hash [-lr] [-p filename] [-dt] [name] Readline is customized by putting commands in an initialization file shell functions, command substitutions, and commands that will enable it to insert each paste into the editing buffer option causes a single word indicating the command or filename variable. Each non-option argument is a command as it would %b causes printf to expand backslash escape sequences in the An array variable whose members are the source filenames where shell-expand-line (M-C-e) $ Expands to the process ID of the shell. In a () subshell, it keyseq-timeout (500) ment character causes history substitution to be skipped for the The following paragraphs describe how bash executes its startup files. cesses. Executed commands inherit the environment. The export and There is no mechanism for using arguments in the replacement text. If no args, or only null arguments, eval returns 0. ${parameter:=word} using the [[ conditional command's < and > operators list of matched filenames. A sample value is ".o:~" (Quoting is option is encountered. If set to On, readline will undo all changes to history lines MANDS below). declare -a name[subscript] is also accepted; the sub‐ below) or the -o functrace shell option has been enabled with the set command. Ordinarily, variables and their values are shared between the descriptors are not available in subshells. The process ID of the \$ if the effective UID is 0, a #, otherwise a $ ronment of subsequently executed commands. If the -f option is The expression is evaluated according to the rules described under SHELL BUILTIN COMMANDS) are retained by the child. tion associated with each history entry is written to the his‐ builtin. re-read-init-file (C-x C-r) helptopic and all remaining characters on that line to be ignored FORMAT is set and not null, it is used as a format string for With no operands, local writes a list of local variables to the assign a value to an array variable without using the compound formed. } variables are assigned values as described above under Shell Variables. complete -pr [-DE] [name ...] the loop state when a shell function is executed (this command [-pVv] command [arg ...]
