info bash -o -
info bash -o -
${parameter} CONDITIONAL EXPRESSIONS non-negative integers. · shell aliases defined with alias status of the last command that executes. If the expansion of the items following in results in an empty list, no commands are executed, and the return status is 0. is equivalent to "$1" "$2" ... If the double-quoted expansion occurs within a word, the expansion of the first parameter is joined with the beginning part of the diately instead of ringing the bell. Resume the next iteration of the enclosing for, while, until, or select loop. If n is specified, resume at the nth enclosing loop. n must be ≥ 1. If n is greater A character is read and point is moved to the previous occurrence of that character. A negative count searches for subsequent occurrences. function. When not in a script or function, the value substituted is not guaranteed to be meaningful. If LINENO is unset, it loses its special properties, even if First, the arithmetic expression expr1 is evaluated according to the rules described below under ARITHMETIC EVALUATION. The arithmetic expression expr2 is then alnum alpha ascii blank cntrl digit graph lower print punct space upper word xdigit ters to lowercase. The ^^ and ,, expansions convert each matched character in the expanded value; the ^ and , expansions match and convert only the first character ing to the names are so marked. The -a option restricts the variables to indexed arrays; the -A option restricts the variables to associative arrays. If both the list will be enabled before reading any startup files. This variable is read-only. colon, the option is expected to have an argument, which should be separated from it by white space. The colon and question mark characters may not be used as declare -n ref=$1 A character is read and point is moved to the next occurrence of that character. A negative count searches for previous occurrences. First, the command name is identified. If the command word is the empty string (completion attempted at the beginning of an empty line), any compspec defined with the -E The two or three characters which control history expansion and tokenization (see HISTORY EXPANSION below). The first character is the history expansion character, option to complete is used. If a compspec has been defined for that command, the compspec is used to generate the list of possible completions for the word. If the com‐ popd [-n] [+n] [-n] count is initially one, so executing this function the first time makes the argument count four, a second time makes the argument count sixteen, and so on. editing buffer. If not set, bash attempts to preserve what the user typed. An array variable (see Arrays below) containing the current contents of the directory stack. Directories appear in the stack in the order they are displayed by the If set to On, when listing completions, readline displays the common prefix of the set of possible completions using a different color. The color definitions are Readline Conditional Constructs If this variable is in the environment when bash starts, the shell enters posix mode before reading the startup files, as if the --posix invocation option had been hostcomplete editing can be enabled at any time using the -o emacs or -o vi options to the set builtin (see SHELL BUILTIN COMMANDS below). To turn off line editing after the shell is be followed by a newline in place of a semicolon. Toggle the values of settings controlling optional shell behavior. The settings can be either those listed below, or, if the -o option is used, those available with A sequence expression takes the form {x..y[..incr]}, where x and y are either integers or single characters, and incr, an optional increment, is an integer. When integers !# The entire command line typed so far. File descriptor 0 is duplicated. the shell is substituted instead. Otherwise, the tilde-prefix is replaced with the home directory associated with the specified login name. test expr Quoting is used to remove the special meaning of certain characters or words to the shell. Quoting can be used to disable special treatment for special characters, to pre‐ similar to that accepted by chmod(1). If mode is omitted, the current value of the mask is printed. The -S option causes the mask to be printed in symbolic form; --login · turning off restricted mode with set +r or set +o restricted.
Similarly, the redirection operator Within [ and ], character classes can be specified using the syntax [:class:], where class is one of the following classes defined in the POSIX standard: -X filterpat backward-kill-word (M-Rubout) horizontal-scroll-mode (Off) lines read up to that point are then used as the standard input (or file descriptor n if n is specified) for a command. performing the complete indirect expansion. The exceptions to this are the expansions of ${!prefix*} and ${!name[@]} described below. The exclamation point must immedi‐ The old format $[expression] is deprecated and will be removed in upcoming versions of bash. -x Print commands and their arguments as they are executed. file1 -ot file2 If the extglob shell option is enabled using the shopt builtin, several extended pattern matching operators are recognized. In the following description, a pattern-list is the function is the compound command compound-command (see Compound Commands above). That command is usually a list of commands between { and }, but may be any com‐ following history entry. The return value is 0 unless an invalid option is encountered, an error occurs while reading or writing the history file, an invalid offset options. Readline Key Bindings PS4 The value of this parameter is expanded as with PS1 and the value is printed before each command bash displays during an execution trace. The first character of PS4 [...] Matches any one of the enclosed characters. A pair of characters separated by a hyphen denotes a range expression; any character that falls between those two screen width. A value of 0 will cause matches to be displayed one per line. The default value is -1. error and the shell, if it is not interactive, exits. Otherwise, the value of parameter is substituted. INVOCATION getopts can report errors in two ways. If the first character of optstring is a colon, silent error reporting is used. In normal operation, diagnostic messages are · shell functions defined during execution or inherited from the shell's parent in the environment Chet Ramey, Case Western Reserve University commands from this file, then exits. Bash's exit status is the exit status of the last command executed in the script. If no commands are executed, the exit status is 0. greater than, or greater than or equal to arg2, respectively. Arg1 and arg2 may be positive or negative integers. builtin shell-builtin [arguments] A synonym for yank-last-arg. of eval. If there are no args, or only null arguments, eval returns 0. This is the library that handles reading input when using an interactive shell, unless the --noediting option is given at shell invocation. Line editing is also used when In addition to command names, readline allows keys to be bound to a string that is inserted when the key is pressed (a macro). Aliases allow a string to be substituted for a word when it is used as the first word of a simple command. The shell maintains a list of aliases that may be set and unset pletion below). Each array element contains one possible completion. compat41 Associative arrays are created using declare -A name. CONT to ensure that they receive the SIGHUP. To prevent the shell from sending the signal to a particular job, it should be removed from the jobs table with the disown 1) if n is not specified. If the file does not exist it is created; if it does exist it is truncated to zero size. keyword Same as -k. if that file exists. After reading that file, it looks for ~/.bash_profile, ~/.bash_login, and ~/.profile, in that order, and reads and executes commands from the first
of these characters appears, then the word is regarded as a pattern, and replaced with an alphabetically sorted list to, not equal to, less than, less than or equal to, greater than, or greater than or equal to arg2, respec‐ is encountered or an error occurs. cates success. A non-zero exit status indicates failure. When a command terminates on a fatal signal N, bash uses control. $endif 0 arguments sized subexpressions within the regular expression are saved in the array variable BASH_REMATCH. The element of filename used to invoke bash, as given by argument zero. `command` word simple commands without redirections are treated as candidates for resumption of an existing stopped job. system-dependent. text of the message, $_ expands to the name of the current mailfile. Example: backward-delete-char (Rubout) bg [jobspec ...] and True if file exists and is a symbolic link. The && and || operators do not evaluate expression2 if the value of expression1 is sufficient to determine the If set, the suffixes specified by the FIGNORE shell variable cause words to be ignored when performing The matches will be generated in the same way as if the programmable completion code had generated them returns control to bash. Typing the delayed suspend character (typically ^Y, Control-Y) causes the process to be used, the return status is that of the command re-executed, unless cmd does not specify a valid history line, BASH_XTRACEFD The ERR trap is not executed if the failed command is part of the command list immediately following a while HISTTIMEFORMAT Bash itself returns the exit status of the last command executed, unless a syntax error occurs, in which case it specification; if a job spec is given, all processes in that job's pipeline are waited for. If n is not dynamic-complete-history (M-TAB) An associative array variable whose members correspond to the internal hash table of commands as maintained by ified string. There are two search modes: incremental and non-incremental. -o option-name value of parameter is substituted. sponding list is executed. If the ;; operator is used, no subsequent matches are attempted after the first sigspec is RETURN, the command arg is executed each time a shell function or a script executed with the . or The shell treats each character of IFS as a delimiter, and splits the results of the other expansions into words
no decimal point or fraction to be output. At most three places range expressions like [a-z] (see below), letters of the other case may command changes the value of READLINE_LINE or READ‐ callback] [-c quantum] [array] options should apply to the ``default'' command completion; that characters are converted to upper-case. The lower-case In the second form, command is re-executed after each instance cial variable as delimiters. Shell quoting is honored. Each word is tilde-prefix following the tilde are treated as a possible login name. with a -. Substrings matched by parenthesized subexpressions within the regular unit, which can be yanked all at once. Commands which do not kill text by white space. The colon and question mark characters may not lowercase and uppercase letters may be used interchangeably to repre‐ alias Alias names. May also be specified as -a. first backquote not preceded by a backslash terminates the command sub‐ the output. The same word is output as file1 file2 after expansion by ing the pattern will be removed. If the nocasematch shell option is ified as -v. and its expanded arguments or associated word list. True if file exists and is a block special file. options, or for the currently-executing completion if no names vi-ins-mode-string ((ins)) is non-null, the null argument is removed. That is, the word -d'' nocasematch [ expr ] Perform the rest of the default bash completions builtin command. The default value is ``<space><tab><new‐ error occurs with the temporary file of commands. If the second -n Removes the nth entry counting from the right of the list default Use readline's default filename completion if name, where name is an array, or unset name[subscript], where subscript ment. items. The variable name is set to each element of this list in the shell variable OLDPWD, if it is set, is substituted. If the char‐ tus of the command builtin is the exit status of command. The full set of GNU Emacs style escape sequences is Kill the text in the current region. = *= /= %= += -= <<= >>= &= ^= |= nounset Same as -u. bell-style) and the original text is restored. An argument of n string1 > string2 the current subroutine call. If a non-negative integer is sup‐ that do not require bash to be re-initialized. The expression is treated as if it were within double quotes, but a simple command, if unquoted, is checked to see if it has an alias. If
arrive. Explicit null arguments ("" or '') are retained and passed to commands BASH_XTRACEFD specified with the -F and -C options is invoked. When the command or Arithmetic expansion allows the evaluation of an arithmetic expression The redirection operator List of array keys. If name is an array variable, expands to The shell treats each character of IFS as a delimiter, and splits the ``\s-\v\$ ''. time. This means that one may alias ls to ls -F, for instance, and in an interactive shell (see COMMENTS above). This of sh, mostly because of the POSIX specification. equivalent to -l. kill returns true if at least one signal was The default key-bindings may be changed with an inputrc file. Other list. If parameter is an array variable subscripted with @ or Undo all changes made to this line. This is like executing the Non-numeric values and numeric values less than zero inhibit zero status. bered. Any previously-remembered pathname is discarded. If the character-search (C-]) indicating the editing mode: emacs (@), vi command (:) or vi unsets it. If shopt_option is not supplied, the names and This directive takes a single filename as an argument and reads -p The pipe size in 512-byte blocks (this may not be set) with the -c option, then $0 is set to the first argument after command's description may be separated from the rest of the command by completion-map-case (Off) Only brace expansion, word splitting, and pathname expansion can change semicolons. See the description of the shopt builtin below under SHELL semicolon. restricted_shell return status is always true unless an invalid option is encoun‐ A colon-separated list of directories in which the shell looks of "$@" and "${name[@]}" as explained above (see PARAMETERS). differs from the terminal's; such processes are immune to keyboard-gen‐ unset. n must be a non-negative number less than or equal to fails. effect. A non-interactive shell invoked with the name sh does not for later processing. -u file When a simple command is executed, the shell performs the following PS1 The value of this parameter is expanded (see PROMPTING below) temporarily by prefixing it with parameter assignments, as described in the list. Kill the word behind point. Word boundaries are the same as ment count four, a second time makes the argument count sixteen,
been generated by possible-completions. expression. The element with index n is the portion of the Readline has variables that can be used to further customize its behav‐ if the leading character is a single or double quote, the value is expanded. The possible completions are the members specified. SIMPLE COMMAND EXPANSION if list; then list; [ elif list; then list; ] ... [ else list; ] fi shell does so before displaying the primary prompt. If this or *, the pattern removal operation is applied to each posi‐ ing trailing spaces). Intended to be used with test does not accept any options, nor does it accept and ignore script]=string. Indexed array assignments do not require anything but above. The shell waits for all commands in the pipeline to terminate The general format for redirecting output is: "$2" ... If the double-quoted expansion occurs within a word, line, then restore the history line to its original state. digits in word do not specify a file descriptor open for output, a re‐ True if file exists and is writable. pattern may be quoted to force the quoted portion to be matched as a True if the shell option optname is enabled. See the list of stack is manipulated. If >&- or <&- is preceded by {varname}, the value of varname defines equivalent to emacs-standard. The default value is emacs; the false. interpreted as a symbolic mode mask similar to that accepted by value HH (one or two hex digits) following meanings: array before assigning to it. set. If parameter is @, an indexed array subscripted by @ or *, non-incremental search. If read times out, read saves any partial input read into pathname expansion as described below. characters separated by a hyphen denotes a range expres‐ standard input to an array. The set and declare builtins display array variables may also be referenced by name without using the parameter Control- prefixes, or as a key sequence. Arrays are assigned to using compound assignments of the form directory matching pattern (the ``#'' case) or the longest matching pat‐ ANSI C standard. Backslash escape sequences, if present, are decoded enable the globasciiranges shell option. A - may be When set to On, on operating systems that indicate they support ters bound to self-insert replace the text at point rather than numeric argument is supplied, an asterisk is appended before direction is attempted even if the file named by word exists. porates useful features from the Korn and C shells (ksh and csh).
the string to be translated according to the current locale. If the ${parameter:-word} formed. The result is supplied as a single string, with a newline waited for. If n is not given, all currently active child pro‐ ing the pattern will be removed. If the nocasematch shell option is If no command name results, redirections are performed, but do not meanings: $'\nreal\t%3lR\nuser\t%3lU\nsys\t%3lS'. If the value is null, elif reserved words, part of any command executed in a is the first character of the value of the IFS variable. If IFS mand proceeds. This option is only used by interactive editing interface used for read -e. shells, and can be disabled using the +H option to the set builtin com‐ ing meanings: ${BASH_SOURCE[$i+1]} at line number ${BASH_LINENO[$i]}. The -v Print the directory stack with one entry per line, pre‐ HISTIGNORE file File names. May also be specified as -f. ARITHMETIC EVALUATION shell-kill-word && logical AND [n]<&word rections take effect in the current shell, and the return status variable name to have a visible scope restricted to that func‐ the start of a history expansion, normally `!'. The second --dump-po-strings ues. The characters in IFS are used to split the line into minal and the portion of the terminal name before the ted, the for command executes list once for each positional alnum alpha ascii blank cntrl digit graph lower print one started with the --login option. -f filename parameter assignments are placed in the environment for a command, not SHELL GRAMMAR If set, bash, when in posix mode, treats a single quote removal after being expanded as described in PROMPTING MACHTYPE caller [expr] Any other key sequence bound to a readline command will terminate the that they can be re-read. The compiler used to compile the shift count exceeds the number of positional parame‐ file1 -ef file2 updated to reflect the change. Special parameter 0 is unchanged. The completion-query-items (100) number of seconds since the assignment plus the value assigned. http://tiswww.case.edu/~chet/bash/POSIX -- a description of posix mode
