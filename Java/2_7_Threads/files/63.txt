info bash -o -
info bash -o -
later use the current locale's collation sequence and strcoll(3). If set to On, menu completion displays the common prefix of the list of possible completions (which may be empty) before cycling through the list. Equivalent to -l. read [-ers] [-a aname] [-d delim] [-i text] [-n nchars] [-N nchars] [-p prompt] [-t timeout] [-u fd] [name ...] onecmd Same as -t. tialized. \? question mark to self-insert replace the text at point rather than pushing the text to the right. Characters bound to backward-delete-char replace the character before point with -w file without regard to the case of alphabetic characters. Note that when using range expressions like [a-z] (see below), letters of the other case may be included, depending on Similar to complete, but replaces the word to be completed with a single match from the list of possible completions. Repeated execution of menu-complete steps string1 = string2 <= >= < > 2. The words that are not variable assignments or redirections are expanded. If any words remain after expansion, the first word is taken to be the name of the command BASH_ARGV (the ``##'' case) deleted. If parameter is @ or *, the pattern removal operation is applied to each positional parameter in turn, and the expansion is the resultant histexpand Pattern substitution. The pattern is expanded to produce a pattern just as in pathname expansion. Parameter is expanded and the longest match of pattern against If set to On, readline will display completions with matches sorted horizontally in alphabetical order, rather than down the screen. C-Meta-u: universal-argument Execute the specified shell builtin, passing it arguments, and return its exit status. This is useful when defining a function whose name is the same as a shell tion of the shopt builtin below), and readline is being used, history substitutions are not immediately passed to the shell parser. Instead, the expanded line is reloaded necessary, to contain no more than the number of lines specified by the value of HISTFILESIZE. If HISTFILESIZE is unset, or set to null, a non-numeric value, or a numeric Moving File Descriptors For example, placing On systems that can support it, there is an additional expansion available: process substitution. This is performed at the same time as tilde, parameter, variable, and If a sigspec is ERR, the command arg is executed whenever a pipeline (which may consist of a single simple command), a list, or a compound command returns a non-zero builtin, retaining the functionality of the builtin within the function. The cd builtin is commonly redefined this way. The return status is false if shell-builtin Equivalent to -D, but the output is in the GNU gettext po (portable object) file format. Remove matching prefix pattern. The word is expanded to produce a pattern just as in pathname expansion. If the pattern matches the beginning of the value of while list-1; do list-2; done When a command terminates on a fatal signal N, bash uses the value of 128+N as the exit status. This creates a coprocess named NAME. If NAME is not supplied, the default name is COPROC. NAME must not be supplied if command is a simple command (see above); otherwise, There are two formats for redirecting standard output and standard error: capitalize-word (M-c) user id is set to the real user id. If the -p option is supplied at invocation, the startup behavior is the same, but the effective user id is not reset. Associative arrays are created using declare -A name. Resume each suspended job jobspec in the background, as if it had been started with &. If jobspec is not present, the shell's notion of the current job is used. bg "\C-xq": "\eb\"\ef\"" continue it in the background, the fg command to continue it in the foreground, or the kill command to kill it. A ^Z takes effect immediately, and has the additional side
also be referred to using a prefix of the name used to start it, or using a substring that appears in its command line. For example, %ce refers to a stopped ce job. If a In a non-interactive shell, or an interactive shell in which the interactive_comments option to the shopt builtin is enabled (see SHELL BUILTIN COMMANDS below), a word cuted. Array variables cannot be given the nameref attribute. However, nameref variables can reference array variables and subscripted array variables. Namerefs can be The shell treats several parameters specially. These parameters may only be referenced; assignment to them is not allowed. HISTORY EXPANSION shift_verbose The systemwide initialization file, executed for login shells MAPFILE If the metafied character x is lowercase, run the command that is bound to the corresponding uppercase character. -i If the -i option is present, the shell is interactive. [n]<<<word \nnn the eight-bit character whose value is the octal value nnn (one to three digits) Insert the character typed. For instance, assuming that there is a library of compspecs, each kept in a file corresponding to the name of the command, the following default completion function would and the expansion is the resultant list. If parameter is an array variable subscripted with @ or *, the substitution operation is applied to each member of the between the point and mark is referred to as the region. Move forward to the end of the next word. Words are composed of alphanumeric characters (letters and digits). [n]<<[-]word filenames searched. If any arguments are supplied, they become the positional parameters when filename is executed. Otherwise the positional parameters are unchanged. If options. When bash is interactive, in the absence of any traps, it ignores SIGTERM (so that kill 0 does not kill an interactive shell), and SIGINT is caught and handled (so that the recreate the current option settings is displayed on the standard output. LINES Used by the select compound command to determine the column length for printing selection lists. Automatically set if the checkwinsize option is enabled or in an out the SIG prefix) or a signal number; signum is a signal number. If sigspec is not present, then SIGTERM is assumed. An argument of -l lists the signal names. An indexed array is created automatically if any variable is assigned to using the syntax name[subscript]=value. The subscript is treated as an arithmetic expression that True if either expr1 or expr2 is true. ing job specification) also refers to the current job. the default output is an octal number. If the -p option is supplied, and mode is omitted, the output is in a form that may be reused as input. The return status is A readonly array variable whose members hold version information for this instance of bash. The values assigned to the array members are as follows: command1 || command2 signal Signal names. SIMPLE COMMAND EXPANSION $ Expands to the process ID of the shell. In a () subshell, it expands to the process ID of the current shell, not the subshell. provides functionality analogous to the %? job identifier (see JOB CONTROL below). If set to any other value, the supplied string must be a prefix of a stopped command below. cdable_vars value of the last (rightmost) command to exit with a non-zero status, or zero if all commands exit successfully. If the reserved word ! precedes a pipeline, the exit sta‐ returns true if at least one signal was successfully sent, or false if an error occurs or an invalid option is encountered. \xHH the eight-bit character whose value is the hexadecimal value HH (one or two hex digits)
Redirecting Input made without an intervening command, the shell does not print another warning, and any stopped jobs are terminated. -a Append the ``new'' history lines to the history file. These are history lines entered since the begin‐ shopt Shell option names as accepted by the shopt builtin. \a alert (bell) xpg_echo of the expression is non-zero, the return status is 0; otherwise the return status is 1. This is exactly with the standard output of the command, with any trailing newlines deleted. Embedded newlines are not deleted, but A readonly array variable whose members hold version information for this instance of bash. The values available, will not have useful values. With no options, display the command history list with line numbers. Lines listed with a 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh have been modi‐ and can change the files the command reads from and writes to. Redirection may also be used to modify file handles Print all of the settable readline variables and their values to the readline output stream. If a numeric \UHHHHHHHH or an underscore. Also referred to as an identifier. COMMAND EXECUTION ENVIRONMENT is less than or equal to 36, lowercase and uppercase letters may be used interchangeably to represent numbers between printf [-v var] format [arguments] · various process IDs, including those of background jobs, the value of $$, and the value of PPID rently does not cause aliases to be removed from the alias list. If BASH_ALIASES is unset, it loses its spe‐ tus is 0. If there is a redirection error, the return status is 1. ignored if it is less than 0 or greater than the terminal screen width. A value of 0 will cause matches to be attempt to match more than one character. The ^ operator converts lowercase letters matching pattern to \xHH the eight-bit character whose value is the hexadecimal value HH (one or two hex digits) history. A negative argument switches the direction through the history (back or forward). The history If the -l option is given, the commands are listed on standard output. Otherwise, the editor given by ename forward-backward-delete-char digits, zero-padding where necessary. When characters are supplied, the expression expands to each character lexico‐ A colon-separated list of directories in which the shell looks for dynamically loadable builtins specified by Use Default Values. If parameter is unset or null, the expansion of word is substituted. Otherwise, the -e If the standard input is coming from a terminal, readline (see READLINE above) is used to obtain the erating the matches. Posix mode, -c and -f, which are in 512-byte increments. The return status is 0 unless an invalid option or its greater than 9 are represented by the lowercase letters, the uppercase letters, @, and _, in that order. If base 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh / % multiplication, division, remainder kill-whole-line Display prompt on standard error, without a trailing newline, before attempting to read any input. The export [-fn] [name[=word]] ... Control-o: "> output" CONDITIONAL EXPRESSIONS ters. ellipsis when displaying possible completions.
In the second form, "keyseq":function-name or macro, keyseq differs only those completions matching word will be displayed. shell-forward-word quent occurrences. -p prompt -o comp-option 1003.1). Bash can be configured to be POSIX-conformant by default. If used with `:s', any delimiter can be used in place of /, and The shell treats several parameters specially. These parameters may Signals ignored upon entry to the shell cannot be trapped or parisons. That is, the current locale's collating errtrace \a alert (bell) expansion of word is assigned to parameter. The value of param‐ errors that cause the shell to exit), and does not reset of the set builtin command can be used as options when the shell is active_comments option to the shopt builtin is enabled (see SHELL enabled, source inherits any trap on DEBUG; if it is not, any standard input (file descriptor 0) if n is not specified. ing on the value of the HISTTIMEFORMAT variable. When a shell with -r Display only running jobs. which bash is running provides these special files, bash will use them; 0 (zero) it is ready to read a command, and the secondary prompt PS2 when it sists of the following. Unless otherwise noted, the values are inher‐ The return value is true unless an invalid option is supplied, -c option specifies quantum. input. This option allows the positional parameters to be | bitwise OR Perform history and alias expansion on the current line. If a command is terminated by the control operator &, the shell exe‐ Bash Reference Manual, Brian Fox and Chet Ramey ning of the zeroth argument passed to command. This is what number of history entries is set to the value of the HISTSIZE privileged If the nocasematch shell option is enabled, the match is per‐ REDIRECTION · the shell's open files, plus any modifications and additions This syntax is also accepted by the declare builtin. Individual array HOME The home directory of the current user; the default argument for RETURN traps (see the description of the trap builtin under SHELL -r Backslash does not act as an escape character. The back‐ dren. Care should be taken in cases where this may cause a problem. line specifies an interpreter for the program. The shell executes the Add this digit to the argument already accumulating, or start a
MACHTYPE to be checked for alias substitution when the alias is expanded. Constants with a leading 0 are interpreted as octal numbers. A leading input-meta (Off) copy-region-as-kill fied file descriptor, non-zero otherwise. The exit sta‐ The matches will be generated in the same way as if the program‐ } The name of the file in which command history is saved (see HIS‐ The shell supports a history expansion feature that is similar to the below). The file descriptors can be utilized as arguments to shell performed using a default value for PATH that is guaranteed to backslash. The backslash preceding the ! is not removed. either. This is active only when bash is using back‐ -r Display only running jobs. The systemwide initialization file, executed for login shells with name arguments, additional options, other than -f and -F, the command, with any trailing newlines deleted. Embedded newlines are is made local to the function in which local is invoked: shell SECONDS, the value returned upon subsequent references is the Each arg is an arithmetic expression to be evaluated (see ARITH‐ hashall Same as -h. more instances of the command builtin and retain these assignment bash. If strict compatibility with sh is desired, start bash with the the terminal's function keys. The word on the right side file can test for a particular value. This could be used If set, range expressions used in pattern matching options to be recognized. bind-tty-special-chars (On) history file is read, lines beginning with the history comment ary. The killed text is saved on the kill-ring. file1 -nt file2 a terminal control sequence into the mode string. ESC, LFD, NEWLINE, RET, RETURN, SPC, SPACE, and TAB. \a alert (bell) to mail that as well! Suggestions and `philosophical' bug reports may been given. This is an exception to the usual printf echo-control-characters (On) completion specifications are printed in a way that allows them Abort the current editing command and ring the terminal's bell are not sorted; left to right order is preserved. For example,
When in posix mode, these builtins may appear in a command after one or · various process IDs, including those of background jobs, the pletions. tution word expansions, or when filtering possible com‐ file1 -ef file2 if no pattern matches. Otherwise, it is the exit status of the options for each name or the current completion. The possible Only brace expansion, word splitting, and pathname expansion can change the value of the mark-directories readline variable, regardless of the command1 || command2 NAME \\ backslash fied with complete -D. It's possible for shell functions executed as value of each array member separated by the first character of the IFS the exceptions that +a may not be used to destroy an array vari‐ After the optional word designator, there may appear a sequence of one tory builtin may be used to display or modify the history list and from the left of the list shown by dirs, starting with as the mark. -C command parameter as the name of the variable; this variable is then expanded sequences by default. default of ~/.inputrc (see READLINE below). ronment. If any of the assignments attempts to assign a value to a If set to On, completed directory names have a slash appended. a question mark (?) is placed in name, OPTARG is unset, and a apply to ``empty'' command completion; that is, completion its value is interpreted as a filename containing commands to Equivalent to -D, but the output is in the GNU gettext po (por‐ --noediting expansion, with an asterisk implicitly appended. This pattern even if it is subsequently reset. 0 and 32767 is generated. The sequence of random numbers may be dump-functions -v varname is enabled. If the -L option is used, the pathname printed may Tildes are expanded in filenames as described below under Tilde Expan‐ standard input (file descriptor 0) is used. The expression is parsed and evaluated according to \t horizontal tab coprocess is executed asynchronously in a subshell, as if the command to be removed from each name. If a variable name is followed by bash [options] [command_string | file] Kill the text from point to the end of the line.
Change the behavior of bash where the default operation differs depends on the number of arguments; see below. Operator prece‐ is given, the commands are listed on standard output. Other‐ without job control. Send the signal named by sigspec or signum to the processes After word splitting, unless the -f option has been set, bash scans than the default, then sequences of the whitespace characters space, invocation, the shell scans its own environment and creates a parameter not be reset. In posix mode, only shell variables are listed. is the resultant list. If parameter is an array variable sub‐ Compound commands and command sequences of the form `a ; b ; c' are not autocd If set, a command name that is the name of a directory the pattern substitution word expansion using quote posix mode affects bash's behavior. · adding or deleting builtin commands with the -f and -d options chown root /usr/{ucb/{ex,edit},lib/{ex?.?*,how_ex}} pletes, the shell prints a line containing its exit sta‐ kill ring. Consecutive kills cause the text to be accumulated into one Uppercase the current (or following) word. With a negative -d The first character of delim is used to terminate each exists but does not have a numeric value, or has no value, the perform history expansion by default. nocaseglob Readline has variables that can be used to further customize its behav‐ key sequence using the input read so far, or can take additional Kill the word behind point. Word boundaries are the same as mark-modified-lines (Off) If set, bash checks that a command found in the hash ta‐ it as a shell variable. a trailing portion of the expanded value of parameter, then the shells, and can be disabled using the +H option to the set builtin com‐ Rotate the kill ring, and yank the new top. Only works follow‐ bind [-m keymap] keyseq:readline-command shell (see INVOCATION above). The value may not be the initialization file is read, and the key bindings and variables are hexadecimal value HHHH (one to four hex digits) scrolling the input horizontally on a single screen line when it # Quote the current or previous word with the -c option, then $0 is set to the first argument after pat] [-W wordlist] [-F function] [-C command] 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh Expands to the positional parameters, starting from one. When An array variable whose members are assigned by the =~ binary versions of sh. sh does not treat opening or closing braces specially If an invalid option is seen, getopts places ? into name and, if complete_fullquote event; the empty string is returned in that case.
