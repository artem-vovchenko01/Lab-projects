info bash -o -
info bash -o -
FUNCNAME An array variable from which bash reads the possible completions generated by a shell function invoked by the programmable completion facility (see Programmable Com‐ typeset [-aAfFgilnrtux] [-p] [name[=value] ...] Expand the line as the shell does. This performs alias and history expansion as well as all of the shell word expansions. See HISTORY EXPANSION below for a shift [n] In this example, C-u is again bound to the function universal-argument. C-x C-r is bound to the function re-read-init-file, and ESC [ 1 1 ~ is bound to insert the text FUNCNAME have no effect. If FUNCNAME is unset, it loses its special properties, even if it is subsequently reset. environment. If -a is supplied, the shell passes name as the zeroth argument to the executed command. If command cannot be executed for some reason, a non-interac‐ BASH(1) General Commands Manual BASH(1) Output the args, separated by spaces, followed by a newline. The return status is 0 unless a write error occurs. If -n is specified, the trailing newline is sup‐ When braces are used, the matching ending brace is the first `}' not escaped by a backslash or within a quoted string, and not within an embedded arithmetic expansion, com‐ ENV, expands its value if it is defined, and uses the expanded value as the name of a file to read and execute. Since a shell invoked as sh does not attempt to read and Set the number of exited child status values for the shell to remember. Bash will not allow this value to be decreased below a POSIX-mandated minimum, and there is is the first character of the value of the IFS variable. If IFS is unset, the parameters are separated by spaces. If IFS is null, the parameters are joined without -l List the names of all readline functions. of the patterns in GLOBIGNORE. The dotglob option is disabled when GLOBIGNORE is unset. The pattern matching honors the setting of the extglob shell option. ${!name[@]} the end of the array, and an index of -1 references the last element. read, but neither rshd nor sshd generally invoke the shell with those options or allow them to be specified. returning its exit status. privileged 0 Expands to the name of the shell or shell script. This is set at shell initialization. If bash is invoked with a file of commands, $0 is set to the name of that output pertaining to jobs (e.g., the output of the jobs command), the current job is always flagged with a +, and the previous job with a -. A single % (with no accompany‐ (see Duplicating File Descriptors below). array beginning at one greater than the array's maximum index (for indexed arrays) or added as additional key-value pairs in an associative array. When applied to a Quick substitution. Repeat the previous command, replacing string1 with string2. Equivalent to ``!!:s/string1/string2/'' (see Modifiers below). Once you have determined that a bug actually exists, use the bashbug command to submit a bug report. If you have a fix, you are encouraged to mail that as well! Sugges‐ This variable controls how the shell interacts with the user and job control. If this variable is set, single word simple commands without redirections are treated completion-query-items (100) dependent. return file. If a command is hashed, -p and -P print the hashed value, which is not necessarily the file that appears first in PATH. If the -a option is used, type List the possible completions of the text before point, treating it as a filename. Each of the metacharacters listed above under DEFINITIONS has special meaning to the shell and must be quoted if it is to represent itself. string1 != string2 shell-expand-line (M-C-e) Refer to the most recent command preceding the current position in the history list starting with string. splitting and null argument removal. end-kbd-macro (C-x )) zero otherwise. The exit status is greater than 128 if the timeout is exceeded. -d delim
-P If set, the shell does not resolve symbolic links when executing commands such as cd that change the current working directory. It uses the physical direc‐ Perform the rest of the default bash completions if the compspec generates no matches. Similar to complete, but replaces the word to be completed with a single match from the list of possible completions. Repeated execution of menu-complete steps local [option] [name[=value] ... | - ] -P prefix -s Display readline key sequences bound to macros and the strings they output in such a way that they can be re-read. word being completed, and the matching words become the possible completions. When used with [[, the < and > operators sort lexicographically using the current locale. -P The maximum number of pseudoterminals If -o is supplied with no option-name, the values of the current options are printed. If +o is supplied with no option-name, a series of set commands to wise, the expression is false. Cause the shell to exit with a status of n. If n is omitted, the exit status is that of the last command executed. A trap on EXIT is executed before the shell ter‐ The result of the expansion is subject to word splitting and pathname expansion as described below. PROMPTING OPTARG The value of the last option argument processed by the getopts builtin command (see SHELL BUILTIN COMMANDS below). possible-hostname-completions (C-x @) copy-region-as-kill is computed, the argument is extracted as if the "!n" history expansion had been specified. Moving File Descriptors expr?expr:expr True if file exists and is a regular file. A compound command is one of the following. In most cases a list in a command's description may be separated from the rest of the command by one or more newlines, and may A correctly-formed brace expansion must contain unquoted opening and closing braces, and at least one unquoted comma or a valid sequence expression. Any incorrectly formed variable will be unset. forward-word (M-f) time the shell is invoked or a shell script is executed. be used in place of /, and the final delimiter is optional if it is the last character of the event line. An a may be used as a synonym for g. subdirectories. \xHH the eight-bit character whose value is the hexadecimal value HH (one or two hex digits) Parameter Expansion ARITHMETIC EVALUATION s/old/new/ syntax. The value of a variable is evaluated as an arithmetic expression when it is referenced, or when a variable which has been given the integer attribute using declare Meta-Rubout: backward-kill-word The number of commands to remember in the command history (see HISTORY below). If the value is 0, commands are not saved in the history list. Numeric values less LINES Used by the select compound command to determine the column length for printing selection lists. Automatically set if the checkwinsize option is enabled or in an completion-prefix-display-length (0) Do not read either the system-wide startup file /etc/profile or any of the personal initialization files ~/.bash_profile, ~/.bash_login, or ~/.profile. By default, The shell treats several parameters specially. These parameters may only be referenced; assignment to them is not allowed. The maximum number of lines contained in the history file. When this variable is assigned a value, the history file is truncated, if necessary, to contain no more job or jobspec specifies a job that was started without job control. · shell aliases defined with alias
or -v option is supplied, the exit status is 0 if command was found, and 1 if not. If neither option is sup‐ enable-bracketed-paste (Off) limit is omitted, the current value of the soft limit of the resource is printed, unless the -H option is in turn. Any numeric argument supplied to these successive calls determines the direction to move through the if necessary, the -P and -S options) should be quoted to protect them from expansion before the complete ifications. The -D option indicates that the remaining options and actions should apply to the ``default'' non-printing characters, which can be used to embed a terminal control sequence into the mode string. The optional l specifies a longer format, including minutes, of the form MMmSS.FFs. The value of p determines · adding or deleting builtin commands with the -f and -d options to the enable builtin command plied, the names and values of the shell options accepted by shopt are printed on the standard output. If ${!name[*]} fied. An argument of n lists only the last n lines. If the shell variable HISTTIMEFORMAT is set and not deleted from the environment. If the value of a parameter in the environment is modified, the new value becomes part using a backslash. The backslash preceding the ! is not removed. list will be enabled before reading any startup files. This variable is read-only. In this section, the Emacs-style notation is used to denote keystrokes. Control keys are denoted by C-key, e.g., C-n according to the rules described below under Pattern Matching, as if the extglob shell option were enabled. The = pletion. This changes the contents of the readline editing buffer. If not set, bash attempts to pre‐ low the left brace in order to introduce indirection. respectively. Unless otherwise noted, the shopt options are disabled (unset) by default. A correctly-formed brace expansion must contain unquoted opening and closing braces, and at least one unquoted comma suspend [-f] and terminated by a control operator. The first word specifies the command to be executed, and is passed as argument status greater than 128, immediately after which the trap is executed. option is given, then -f is assumed. Values are in 1024-byte increments, except for -t, which is in seconds; tuses from shell builtins and compound commands are also limited to this range. Under certain circumstances, the Lines beginning with a # are comments. Lines beginning with a $ indicate conditional constructs. Other lines denote expansion is not used (see Arithmetic Expansion below). Word splitting is not performed, with the exception of "$@" integer attribute has been set, value is evaluated as an arithmetic expression and added to the variable's current ters from the completion that match characters after point in the word being completed, so portions of the Move back a character. << >> left and right bitwise shifts removed from the list. A leading ! in filterpat negates the pattern; in this case, any completion not if [ -n "$BASH_ENV" ]; then . "$BASH_ENV"; fi mkdir /usr/local/src/bash/{old,new,dist,bugs} @ Expands to the positional parameters, starting from one. When the expansion occurs within double quotes, each Negative subscripts to indexed arrays are interpreted as described above. Care must be taken to avoid unwanted side "\C-u": universal-argument True if both expression1 and expression2 are true. !! Refer to the previous command. This is a synonym for `!-1'. When using the second form, word may not expand to a number or -. If it does, other redirection operators apply (see metacharacter-separated words surrounded by quotes are considered one word. History expansions are introduced by the COMMANDS below). Each of the metacharacters listed above under DEFINITIONS has special meaning to the shell and must be quoted if it more arguments.
asterisk is appended before pathname expansion. below). The file descriptors can be utilized as arguments to shell file in the current directory, and, if no file is found, then the shell mands, $0 is set to the name of that file. If bash is started meaning within double quotes. The backslash retains its special mean‐ %[p][l]R The elapsed time in seconds. command list. Bash versions 3.2 and earlier continue [n]<&word facilities, while available, will not have useful values. upcoming versions of bash. complete -D -F _completion_loader -o bashdefault -o default OPTERR is initialized to 1 each time the shell is invoked or a trap handler. if return is executed during a DEBUG trap, the ${parameter:+word} · the file creation mode mask as set by umask or inherited from from the shell. If a sigspec is DEBUG, the command arg is exe‐ backward-delete-char (Rubout) number of seconds since the epoch. Two special argument JOB CONTROL hash [-lr] [-p filename] [-dt] [name] for which no completion has previously been defined. The -E The individual login shell cleanup file, executed when a login functions) in turn. If none of these produces a match, filename If set, and job control is not active, the shell runs directories specified by the cd command. A sample value is arguments to the invoked command. tions specified by the command (see REDIRECTION below). If |& is used, show-all-if-unmodified (Off) may not be changed by subsequent assignment. If the -f option Kill from point to the end of the current word, or if between expands to a separate word. That is, "$@" is equivalent to "$1" unsetting it will result in the standard error being closed. Stop saving the characters typed into the current keyboard macro variable are defined. The shell function ${FUNCNAME[$i]} is shell exits No effect; the command does nothing beyond expanding arguments True if file exists and is owned by the effective group id. ${parameter} mapfile [-d delim] [-n count] [-O origin] [-s count] [-t] [-u fd] [-C or one started with the -i option. PS1 is set and $- includes i if if the user so specifies with stty tostop, write to the terminal. is available. Use the \1 and \2 escapes to begin and end A simple command is a sequence of optional variable assignments fol‐
have the following meanings: BASH_ALIASES added as additional key-value pairs in an associative array. When -u When the variable is assigned a value, all lower-case able and +r will not remove the readonly attribute. When used with -P, and the current working directory cannot be success‐ the set builtin command. Positional parameters may not be assigned to Names of shell functions. argument is supplied, array is invalid or unassignable, or if When used with [[, the < and > operators sort lexicographically file whose name is the expansion of word. functrace even if it is subsequently reset. mand (see SHELL BUILTIN COMMANDS below). Non-interactive shells do not performed unless an ! appearing in double quotes is escaped using a zero. If set to On, readline will display completions with matches point over that word as well. If point is at the end of the able) shown by dirs, starting with zero. For example: ``popd of BASH_REMATCH with index 0 is the portion of the string matching the Undo all changes made to this line. This is like executing the $#. If n is 0, no parameters are changed. If n is not given, to continue it in the foreground, or the kill command to kill it. A ^Z BASHPID exactly like yank-nth-arg. Successive calls to yank-last-arg unset, or does not include a valid value, all lines read by the This directive takes a single filename as an argument and reads ~/.bash_profile is one of /dev/stdin, /dev/stdout, or /dev/stderr, file descriptor 0, ? Matches any single character. /dev/fd/fd matches more than one job, bash reports an error. Using %?ce, on the words. Various modifiers are available to manipulate the selected bash creates temporary files for the shell's use. line command in the same history entry. This allows shell. Also known as a token. current version. If BASH_COMPAT is set to a value that is not [[ conditional commands, when performing pattern substi‐ expansion mechanism (see the description of histchars above under Shell ence. %[p][l]U The number of CPU seconds spent in user mode. entered. When shell-command is executed, the shell sets supplied, each name refers to a variable; if there is no vari‐ Each of the metacharacters listed above under DEFINITIONS has special Shell builtin commands and functions are not stoppable/restartable.
brackets. If the pattern is stored in a shell variable, quoting the xpg_echo ment-begin variable is inserted at the beginning of the current option is enabled by default. corresponding argument in the same way as echo -e. put was redirected to dirlist. declare builtin command (see declare below in SHELL BUILTIN COMMANDS). tokenization (see HISTORY EXPANSION below). The first character is the history expansion character, the character which signals a The expansion is a string consisting of flag values rep‐ this variable is in the environment when bash starts up, each Once you have determined that a bug actually exists, use the bashbug fully determined after a successful directory change, cd will assignments to PATH, MAILPATH, and CDPATH, and the shell assigns the executed in a subshell environment. The DEBUG and of zero. tiguously. Indexed arrays are referenced using integers (including Duplicating File Descriptors DEFINITIONS possible-completions (M-?) established between the executing shell and the coprocess. Move to the first line in the history. exit status of list. Display helpful information about builtin commands. If pattern digit-argument (M-0, M-1, ..., M--) name will be unset rather than the variable it references. -n Subshells spawned to execute command substitutions inherit the value of The shell does not exit if the command that fails is parameter assignments are placed in the environment for a command, not user User names. May also be specified as -u. pletions against the word being completed is performed; the function or ing only when followed by one of the following characters: $, `, ", \, (see Modifiers below). current version. The valid compatibility levels correspond to Substrings matched by parenthesized subexpressions within the regular filename completion is disabled. If the -o bashdefault option was sup‐ 1 each time the shell or a shell script is invoked. When an PIPESTATUS % The word matched by the most recent `?string?' search. re-read-init-file (C-x C-r) /dev/tcp/host/port tildes). or job, the return status is 127. Otherwise, the return status it provides for filing a bug report. cial variable as delimiters. Shell quoting is honored. Each word is more slashes, the shell executes the named program in a separate execu‐ instead of the shell builtin version, run ``enable -n test''.
comparison tiguously. Indexed arrays are referenced using integers (including appearing in BASHOPTS are those reported as on by shopt. If The shell sets this option if it is started in -n Displays the nth entry counting from the right of the $, `, and = and any of the shell metacharacters or quoting characters | & ; ( ) < > space tab newline use during substitution. The second is to select portions of that line -f Disable pathname expansion. parameter as described above PARAMETERS) or an array reference READLINE_POINT The expression is false. name of a nameref variable as an argument, the variable referenced by cuted, the parameters supplied are pushed onto BASH_ARGV. The checkhash mable completion facilities (see Programmable Completion below). OPTERR If set to the value 1, bash displays error messages generated by added as additional key-value pairs in an associative array. When If the first argument is !, the expression is true if and remaining options should apply to ``empty'' command completion; consecutive EOF characters which must be typed as the first GROUPS An array variable containing the list of groups of which the the output is formatted in such a way that it can be made part \xHH the eight-bit character whose value is the hexadecimal the local command, unless the -g option is supplied. If a vari‐ as the zeroth argument to the executed command. If command can‐ A colon-separated list of shell patterns (see Pattern Matching) printed. The -S option causes the mask to be printed in sym‐ port named pipes (FIFOs) or the /dev/fd method of naming open files. fixed with 0 to force each term to have the same width. When either x from the right of the list shown by dirs, starting with the list of array indices (keys) assigned in name. If name is character-search-backward (M-C-]) long as the whitespace character is in the value of IFS (an IFS white‐ recursive calls. popd [-n] [+n] [-n] matching pattern (the ``%%'' case) deleted. If parameter is @ Pathname Expansion with - without requiring --. Other builtins that accept arguments but option processing, then commands are read from the standard command to submit a bug report. If you have a fix, you are encouraged null. mable completion facilities (see Programmable Completion below). exactly ( and the third argument is exactly ), the result Automatically set to a string that fully describes the system list is executed and the command completes. Otherwise, the else decreasing order of precedence: after the shell is running, use the +o emacs or +o vi options to the
