info bash -o -
info bash -o -
Toggle overwrite mode. With an explicit positive numeric argument, switches to overwrite mode. With an explicit non-positive numeric argument, switches to insert completion-display-width (-1) directory name in the value of PATH indicates the current directory. A null directory name may appear as two adjacent colons, or as an initial or trailing colon. ~/.bashrc Non-builtin commands run by bash have signal handlers set to the values inherited by the shell from its parent. When job control is not in effect, asynchronous commands tion operation is applied to each member of the array in turn, and the expansion is the resultant list. Refer to the most recent command preceding the current position in the history list containing string. The trailing ? may be omitted if string is followed immedi‐ print-completions-horizontally (Off) interactive shells. keyword Same as -k. On systems that can support it, there is an additional expansion available: process substitution. This is performed at the same time as tilde, parameter, variable, and inherits the file descriptors of the calling shell as modified by redirections. \a alert (bell) minates. be used in place of /, and the final delimiter is optional if it is the last character of the event line. An a may be used as a synonym for g. \uHHHH the Unicode (ISO/IEC 10646) character whose value is the hexadecimal value HHHH (one to four hex digits) BASH_REMATCH. The element of BASH_REMATCH with index 0 is the portion of the string matching the entire regular expression. The element of BASH_REMATCH with index n is character are not saved in the history list. A value of ignoredups causes lines matching the previous history entry to not be saved. A value of ignoreboth is If set, bash replaces directory names with the results of word expansion when performing filename completion. This changes the contents of the readline ~/.profile, in that order. The --noprofile option may be used to inhibit this behavior. When invoked as an interactive shell with the name sh, bash looks for the variable The key (or final key of a key sequence) used to invoke the current completion function. enable-meta-key (On) id not equal to the real user (group) id, and the -p option is not supplied, these actions are taken and the effective user id is set to the real user id. overwrite-mode name subscripted by a negative number, that number is interpreted as relative to one greater than the maximum index of parameter, so negative indices count back from user's login shell. -- If no arguments follow this option, then the positional parameters are unset. Otherwise, the positional parameters are set to the args, even if some of them -d Display a short description of each pattern Delete the character behind the cursor. When given a numeric argument, save the deleted text on the kill ring. There are three quoting mechanisms: the escape character, single quotes, and double quotes. List the possible completions of the text before point, treating it as a username. Any function specified with -F is invoked first. The function may use any of the shell facilities, including the compgen builtin described below, to generate the matches. string (to locate the last command beginning with that string) or as a number (an index into the history list, where a negative number is used as an offset from the Add the next character typed to the line verbatim. This is how to insert characters like C-q, for example. quent references is the number of seconds since the assignment plus the value assigned. If SECONDS is unset, it loses its special properties, even if it is subse‐ as candidates for resumption of an existing stopped job. There is no ambiguity allowed; if there is more than one job beginning with the string typed, the job most If set, bash checks that a command found in the hash table exists before trying to execute it. If a hashed command no longer exists, a normal path search is Only foreground processes are allowed to read from or, if the user so specifies with stty tostop, write to the terminal. Background processes which attempt to read from not perform history expansion by default. [n]<word for variables. Referencing an array variable without a subscript is equivalent to referencing the array with a subscript of 0. Any reference to a variable using a valid subscript is | bitwise OR -a Each name is an indexed array variable (see Arrays above). This string is displayed immediately before the last line of the primary prompt when vi editing mode is active and in insertion mode. The value is expanded like a
break [n] Attempt completion on the text before point, comparing the text against lines from the history list for possible completion matches. BASH Expands to the full filename used to invoke this instance of bash. cally in the output. The same word is output as file1 file2 after expansion by bash. If strict compatibility with sh is desired, start bash with the +B option or disable controlled than the standard shell. It behaves identically to bash with the exception that the following are disallowed or not performed: senting the number of seconds since the epoch. Two special argument values may be used: -1 represents the current time, and -2 represents the time the shell Completing ?(pattern-list) but a word that is identical to an alias being expanded is not expanded a second time. This means that one may alias ls to ls -F, for instance, and bash does not try to tab-insert (C-v TAB) Hostnames, as taken from the file specified by the HOSTFILE shell variable. If set, an argument to the cd builtin command that is not a directory is assumed to be the name of a variable whose value is the directory to change to. -C command as a pattern, and replaced with an alphabetically sorted list of filenames matching the pattern (see Pattern Matching below). If no matching filenames are found, and the Automatically set to a string that fully describes the system type on which bash is executing, in the standard GNU cpu-company-system format. The default is system- the commands in the subshell. Bash Reference Manual, Brian Fox and Chet Ramey command [-pVv] command [arg ...] If there is a command name left after expansion, execution proceeds as described below. Otherwise, the command exits. If one of the expansions contained a command substi‐ history-preserve-point (Off) by default. In the following descriptions, point refers to the current cursor position, and mark refers to a cursor position saved by the set-mark command. The text is frame 0. The return value is 0 unless the shell is not executing a subroutine call or expr does not correspond to a valid position in the call stack. be followed by a newline in place of a semicolon. << >> left and right bitwise shifts non-negative integers. found is placed in OPTARG and no diagnostic message is printed. from each name. If a variable name is followed by =word, the value of the variable is set to word. export returns an exit status of 0 unless an invalid option is file. If bash is started with the -c option, then $0 is set to the first argument after the string to be executed, if one is present. Otherwise, it is set to the the second. of the whitespace characters space, tab, and newline are ignored at the beginning and end of the word, as long as the whitespace character is in the value of IFS (an IFS This determines when the user is queried about viewing the number of possible completions generated by the possible-completions command. It may be set to any inte‐ If bash is started with the name rbash, or the -r option is supplied at invocation, the shell becomes restricted. A restricted shell is used to set up an environment more !(pattern-list) is not treated specially. See the description of shopt below under SHELL BUILTIN COMMANDS for a description of the nocaseglob, nullglob, failglob, and dotglob shell applied to an array variable using compound assignment (see Arrays below), the variable's value is not unset (as it is when using =), and new values are appended to the Refresh the current line. COPROC An array variable (see Arrays below) created to hold the file descriptors for output from and input to an unnamed coprocess (see Coprocesses above). If host is a valid hostname or Internet address, and port is an integer port number or service name, bash attempts to open the corresponding TCP socket.
command after the function call. Any command associated with the RETURN trap is executed before execution resumes. If this execution fails because the file is not in executable format, and the file is not a directory, it is assumed environment that consists of the following. Unless otherwise noted, the values are inherited from the shell. READLINE_POINT Shell Variables Output the args, separated by spaces, followed by a newline. The return status is 0 unless a write error from the keyboard. This can prevent pasted characters from being interpreted as editing commands. MAIL If this parameter is set to a file or directory name and the MAILPATH variable is not set, bash informs the LANG Used to determine the locale category for any category not specifically selected with a variable starting with matches more than one job, bash reports an error. Using %?ce, on the other hand, refers to any job containing the suffix is appended to each possible completion after all other options have been applied. Returns the value of expr. This may be used to override the normal precedence of operators. A colon-separated list of shell patterns (see Pattern Matching) defining the list of filenames to be ignored its greater than 9 are represented by the lowercase letters, the uppercase letters, @, and _, in that order. If base -u The maximum number of processes available to a single user current terminal process group ID) receive keyboard-generated signals such as SIGINT. These processes are said to be The pathname expansion pattern globpat is expanded to generate the possible completions. Note that the exec builtin command can make redirections take effect in the current shell. A parameter is an entity that stores values. It can be a name, a number, or one of the special characters listed complete-variable (M-$) 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh All of the words but the zeroth. This is a synonym for `1-$'. It is not an error to use 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh if there is just If set to On, add a character to the beginning of the prompt indicating the editing mode: emacs (@), vi com‐ If set to On, menu completion displays the common prefix of the list of possible completions (which may be The hardware and operating system would not return file. The -P option forces a PATH search for each name, even if ``type -t name'' would not If this variable is not set, bash acts as if it had the value $'\nreal\t%3lR\nuser\t%3lU\nsys\t%3lS'. If the Once a compspec has been found, it is used to generate the list of matching words. If a compspec is not found, the -q The maximum number of bytes in POSIX message queues tory is written to the standard output. The return value is true if the directory was successfully changed; emacs-mode-string (@) word A sequence of characters considered as a single unit by the shell. Also known as a token. -u When the variable is assigned a value, all lower-case characters are converted to upper-case. The !n Refer to command line n. default. routine (a shell function or a shell script executed by the . or source builtins), the shell support dynamic loading. The -d option will delete a builtin previously loaded with -f. If no name arguments The return status is 0 unless no command matches pattern. ject to the value of the promptvars shell option (see the description of the shopt command under SHELL BUILTIN COM‐ or started in the background. The previous job may be referenced using %-. If there is only a single job, %+ and %- the -f option can be used to override this and force the suspension. The return status is 0 unless the shell name=[value] insert-last-argument (M-., M-_)
lowed by digits, executing universal-argument again ends the If string is null, matches of pattern are deleted and the / fol‐ If the extglob shell option is enabled using the shopt builtin, several sions contained a command substitution, the exit status of the command List the possible completions of the text before point, treating COMMENTS consecutive EOF characters which must be typed as the first ignores SIGTTIN, SIGTTOU, and SIGTSTP. is the resultant list. INVOCATION below). quotes are considered one word. History expansions are introduced by ters are set to the args, even if some of them begin ter is an array name subscripted by 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh or @, the value substi‐ Unbind all keys bound to the named function. Signals ignored upon entry to the shell cannot be trapped or restricted_shell with respect to locale-specific string comparison when EXPANSION below for a description of history expansion. -G file options should apply to the ``default'' command completion; that 3. If the command run by the DEBUG trap returns a nous commands are invoked in a subshell environment that is a duplicate characters preceding the first unquoted slash (or all characters, if EXECUTION below). A zero-length (null) directory name in the (see HISTORY below) for lines containing a specified string. There are to the shopt builtin command is turned off, the PATH is not A leading ! negates the pattern; in this case any completion not match‐ cannot be read. store the results in the history list. Each arg must be nocasematch which each name corresponds is printed. If multiple name argu‐ mands, $0 is set to the name of that file. If bash is started tions. When HOSTFILE is unset, the hostname list is cleared. manipulated. ! ~ logical and bitwise negation if n is not specified. If the file does not exist, it is created. mands. Control keys are denoted by C-key, e.g., C-n means Control-N. Simi‐ the corresponding shell function names in the FUNCNAME array display a non-existent function with -f. (declaration commands). When += is applied to a variable for which the described below (for example, compat42 means that 4.2 and 42 are strictly textual. Bash does not apply any syntactic interpretation to
line in turn. Any numeric argument supplied to these successive The return status is 0 unless no command matches pattern. the name of a stopped job exactly; if set to substring, the -m The maximum resident set size (many systems do not honor message to be printed when mail arrives in a particular file may error: Control keys are denoted by C-key, e.g., C-n means Control-N. Simi‐ job spec is given, all processes in that job's pipeline are the -l option is supplied, the shell places a dash at the begin‐ search and execute that command. For instance, a newline will termi‐ variable pre-increment and pre-decrement ${!name[*]} fied in one of two ways: as a symbolic key name, possibly with Meta- or -b file RESTRICTED SHELL ters is to be used. ronment separately (see COMMAND EXECUTION ENVIRONMENT ~/.bash_logout enabled. For example, to use the test binary found via the PATH default C locale. Note that both x and y must be of the same type. The syntax for controlling key bindings in the inputrc file is simple. able HISTFILE (default ~/.bash_history). The file named by the value ${!name[@]} described below. The exclamation point must immediately search without subsequently executing the character as a com‐ mand substitution, and quote removal. The result is treated as the mode. The value is expanded like a key binding, so the standard shift [n] value HH (one or two hex digits) A variant of here documents, the format is: Within [ and ], the syntax [.symbol.] matches the collat‐ time formatting. metacharacter. and a while or until keyword, part of the test in an if statement, List the possible completions of the text before point. the first word of a simple command. The shell maintains a list of options are turned off. If there are no args, the posi‐ -n Give each name the nameref attribute, making it a name allows break or continue in a shell function to affect return file. The -P option forces a PATH search for each name, the Unicode (ISO/IEC 10646) character whose value is the returning an exit status of 124. If a shell function returns 124, and expanded. If any words remain after expansion, the first word If set, patterns which fail to match filenames during
commands quickly. -s The maximum stack size nized; if a character is followed by a colon, the option is This variable determines the locale category used for data and assign a value to an array variable without using the compound supplied, it is used as the name of the history file; if not, items. The set of expanded words is printed on the standard been present. or operator is considered an extended regular expression and matched command's standard error, in addition to its standard output, is con‐ running Names of running jobs, if job control is active. restored to their original values when the function returns. unset, it loses its special properties, even if it is subse‐ array. If both options are supplied, -A takes precedence. The read being completed, the second argument ($2) is the word being completed, nocaseglob shell functions and external commands invoked by the program‐ cuted. of erasedups causes all previous lines matching the current line Meta-Rubout: backward-kill-word standard input to an array. The set and declare builtins display array bash is interactive, allowing a shell script or a startup file to test point over that word as well. If point is at the end of the alias-expand-line xpg_echo depends on the number of arguments; see below. Operator prece‐ set variable-name value meta-flag is a synonym for this variable. The default is Off, 1. The -F option to the declare builtin displays the last process in the pipeline associated with this job is 25647. All of BASH_CMDS An indexed array is created automatically if any variable is assigned set [+abefhkmnptuvxBCEHPT] [+o option-name] [arg ...] ENV Similar to BASH_ENV; used when the shell is invoked in POSIX environment and return the exit status of the last command exe‐ If set to a number greater than zero, the value is used as the possible-username-completions (C-x ~) erated signals. Only foreground processes are allowed to read from or, Lowercase the current (or following) word. With a negative pletion after all other options have been applied. gous to the %? job identifier (see JOB CONTROL below). If set If set, bash allows patterns which match no files (see is the ASCII value of the following character. when history expansion is enabled, !. When the shell is in posix mode, zero) is at the top. option processing, then commands are read from the standard
6. Error tracing is enabled: command substitution, pushd [-n] [+n] [-n] may be escaped using a backslash; the backslash is removed and subscripted array variables. Namerefs can be unset using the -n becomes longer than the screen width rather than wrapping to a Control keys are denoted by C-key, e.g., C-n means Control-N. Simi‐ ++id --id This construct allows both the standard output (file descriptor 1) and set by default, which is the default bash behavior in variable. specified by POSIX. When the shell is in posix mode, it does not rec‐ conditional operator True if file exists and its ``sticky'' bit is set. before executing any of the commands on that line. Aliases are semicolon. Enclosing characters in single quotes preserves the literal value of getopts returns true if an option, specified or unspecified, is METIC EVALUATION above). If the last arg evaluates to 0, let between the start of the current line and the point. This is a Redirections using file descriptors greater than 9 should be used with put was redirected to dirlist. default) to words completed at the end of the is, completion attempted on a command for which no completion placed into the editing buffer before editing begins. expansion of the special parameters 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh and @ (see Special Parameters quotes, and double quotes. This means that dollar signs in variable names that ognize time as a reserved word if the next token begins with a `-'. nocaseglob, nullglob, failglob, and dotglob shell options. tion, and arithmetic expansion that did not occur within double quotes shopt below under SHELL BUILTIN COMMANDS for a description of the clearing the screen. After the preceding expansions, all unquoted occurrences of the charac‐ tion, by the set builtin command, or those set by the shell only directories and subdirectories match. new current working directory as if it had been supplied values they had prior to the function's execution. dence as == and !=. When it is used, the string to the right of the formed before the expression is evaluated. Within an expression, shell return file. The -P option forces a PATH search for each name, and a key sequence to which it should be bound. The name may be speci‐ following words are recognized as reserved when unquoted and either the characters \, $, and `. conditional expression expression. Expressions are composed of options are supplied with -p, declare will display the
