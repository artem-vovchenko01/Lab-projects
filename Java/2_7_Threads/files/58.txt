info bash -o -
info bash -o -
%b causes printf to expand backslash escape sequences in the corresponding argument in the same way as echo -e. When assigning to an associative array, the subscript is required. errexit Same as -e. ${parameter/pattern/string} [time [-p]] [ ! ] command [ [|⎪|&] command2 ... ] If set, the source (.) builtin uses the value of PATH to find the directory containing the file supplied as an argument. This option is enabled by default. True if file exists and is executable. · shell parameters that are set by variable assignment or with set or inherited from the shell's parent in the environment Several shell options settable with the shopt builtin may be used to tailor the behavior of history expansion. If the histverify shell option is enabled (see the descrip‐ It must put the possible completions in the COMPREPLY array variable, one per array element. Pathname Expansion True if file1 and file2 refer to the same device and inode numbers. Wait for each specified child process and return its termination status. Each n may be a process ID or a job specification; if a job spec is given, all processes in arithmetic expansion and command substitution. option; +O unsets it. If shopt_option is not supplied, the names and values of the shell options accepted by shopt are printed on the standard output. If the ${parameter:+word} mand word is a full pathname, a compspec for the full pathname is searched for first. If no compspec is found for the full pathname, an attempt is made to find a compspec escape sequences, if present, are decoded as follows: editing was not previously active) editing settings. as a single pattern will match all files and zero or more directories and subdirectories. If followed by a /, two adjacent *s will match only directories and which the integer attribute has been set, value is evaluated as an arithmetic expression and added to the variable's current value, which is also evaluated. When += is set variable-name value If the builtin command return is executed in a function, the function completes and execution resumes with the next command after the function call. Any command associated GNU Bash 4.4 2016 August 26 BASH(1) e Remove all but the trailing suffix. suspension. The return status is 0 unless the shell is a login shell and -f is not supplied, or if job control is not enabled. sion of word. mand (see SHELL GRAMMAR below) or the third word of a case or for command: metacharacter not apply any syntactic interpretation to the context of the expansion or the text between the braces. An indexed array is created automatically if any variable is assigned to using the syntax name[subscript]=value. The subscript is treated as an arithmetic expression that option characters. Each time it is invoked, getopts places the next option in the shell variable name, initializing name if it does not exist, and the index of the members of the resultant list which match the word being completed. Matches one of the given patterns 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh / % multiplication, division, remainder directs only the standard output to file dirlist, because the standard error was duplicated from the standard output before the standard output was redirected to dirlist. quote removal. Each pattern examined is expanded using tilde expansion, parameter and variable expansion, arithmetic expansion, command substitution, and process
command, plus any additions via the export and declare -x commands. causes the file whose name is the expansion of word to be opened for both reading and writing on file descriptor n, or on file descriptor 0 if n is not specified. If the it can be made part of an inputrc file. for (( expr1 ; expr2 ; expr3 )) ; do list ; done local [option] [name[=value] ... | - ] Chet Ramey, Case Western Reserve University write the matches to the standard output. When using the -F or -C options, the various shell variables set by the programmable completion facilities, while avail‐ ${parameter} Show version information for this instance of bash on the standard output and exit successfully. sion. Each character in the expanded value of parameter is tested against pattern, and, if it matches the pattern, its case is converted. The pattern should not parameter, so an offset of -1 evaluates to the last positional parameter. It is an expansion error if length evaluates to a number less than zero. comparison LC_CTYPE Any element of an array may be referenced using ${name[subscript]}. The braces are required to avoid conflicts with pathname expansion. If subscript is @ or *, the word match. Any completion that matches the pattern will be removed from the list. A leading ! negates the pattern; in this case any completion not matching the pattern will -t timeout command executed, or zero if no condition tested true. extglob If set, the extended pattern matching features described above under Pathname Expansion are enabled. True if the shell option optname is enabled. See the list of options under the description of the -o option to the set builtin below. The number of commands to remember in the command history (see HISTORY below). If the value is 0, commands are not saved in the history list. Numeric values less set variable-name value If set, bash does not print a warning message if an attempt is made to use a quoted compound array assignment as an argument to declare, makes word expansion definition is zero unless a syntax error occurs or a readonly function with the same name already exists. When executed, the exit status of a function is the exit ${parameter/pattern/string} special parameters 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh and @ (see Special Parameters above). ${#name[subscript]} expands to the length of ${name[subscript]}. If subscript is 100.txt 101.txt 102.txt 103.txt 104.txt 105.txt 106.txt 107.txt 108.txt 109.txt 10.txt 110.txt 111.txt 112.txt 113.txt 114.txt 115.txt 116.txt 117.txt 118.txt 119.txt 11.txt 120.txt 121.txt 122.txt 123.txt 124.txt 125.txt 126.txt 127.txt 128.txt 12.txt 13.txt 14.txt 15.txt 16.txt 17.txt 18.txt 19.txt 1.txt 20.txt 21.txt 22.txt 23.txt 24.txt 25.txt 26.txt 27.txt 28.txt 29.txt 2.txt 30.txt 31.txt 32.txt 33.txt 34.txt 35.txt 36.txt 37.txt 38.txt 39.txt 3.txt 40.txt 41.txt 42.txt 43.txt 44.txt 45.txt 46.txt 47.txt 48.txt 49.txt 4.txt 50.txt 51.txt 52.txt 53.txt 54.txt 55.txt 56.txt 57.txt 58.txt 59.txt 5.txt 60.txt 61.txt 62.txt 63.txt 64.txt 65.txt 66.txt 67.txt 68.txt 69.txt 6.txt 70.txt 71.txt 72.txt 73.txt 74.txt 75.txt 76.txt 77.txt 78.txt 79.txt 7.txt 80.txt 81.txt 82.txt 83.txt 84.txt 85.txt 86.txt 87.txt 88.txt 89.txt 8.txt 90.txt 91.txt 92.txt 93.txt 94.txt 95.txt 96.txt 97.txt 98.txt 99.txt 9.txt SH.sh or @, the expansion is the reset the loop state when a shell function is executed (this allows break or continue in a shell function to affect loops in the caller's context). status of the last command executed in the body. (See FUNCTIONS below.) redraw-current-line full pathname, even though the shell normally searches for builtins before disk commands. If -n is used, each name is disabled; otherwise, names are enabled. For +n Removes the nth entry counting from the left of the list shown by dirs, starting with zero. For example: ``popd +0'' removes the first directory, ``popd +1'' Refresh the current line. separated by single spaces. which are converted and copied to the standard output, and format specifications, each of which causes printing of the next successive argument. In addition to the and name is a variable with the nameref attribute, name will be unset rather than the variable it references. -n has no effect if the -f option is supplied. If no The GLOBIGNORE shell variable may be used to restrict the set of filenames matching a pattern. If GLOBIGNORE is set, each matching filename that also matches one of the supplied, the expression expands to each character lexicographically between x and y, inclusive, using the default C locale. Note that both x and y must be of the same complete-hostname (M-@) printf [-v var] format [arguments] -h Remember the location of commands as they are looked up for execution. This is enabled by default. lastpipe must be typed as the first characters on an input line before bash exits. If the variable exists but does not have a numeric value, or has no value, the default
If bash is waiting for a command to complete and receives a signal for which a trap has been set, the trap will not If the substitution appears within double quotes, word splitting and pathname expansion are not performed on the REDIRECTION -c file the end of the line, behaves identically to possible-completions. This command is unbound by default. Fetch the next command from the history list, moving forward in the list. Remove matching prefix pattern. The word is expanded to produce a pattern just as in pathname expansion. If Move to the end of the input history, i.e., the line currently being entered. If set to On, completed names which are symbolic links to directories have a slash appended (subject to the directly from a completion specification with the same flags. If word is specified, only those completions string1 < string2 allows key bindings and variable settings to be performed as the result of tests. There are four parser directives When a command is described as killing text, the text deleted is saved for possible future retrieval (yanking). The `command` COMP_WORDBREAKS is unset, it loses its special properties, even if it is subsequently reset. - + unary minus and plus is printed. lent to On. All other values are equivalent to Off. The variables and their default values are: \uHHHH the Unicode (ISO/IEC 10646) character whose value is the hexadecimal value HHHH (one to four hex dig‐ to add and remove directories. Assignment to this variable will not change the current directory. If for destination directories specified by the cd command. A sample value is ".:~:/usr". for re-running the previous command entered, substituting one string for another in the command. The default Suspend the execution of this shell until it receives a SIGCONT signal. A login shell cannot be suspended; provides functionality analogous to the %? job identifier (see JOB CONTROL below). If set to any other If a word begins with an unquoted tilde character (`~'), all of the characters preceding the first unquoted slash (or to a single word with the value of each parameter separated by the first character of the IFS special vari‐ ning of an empty line), any compspec defined with the -E option to complete is used. If a compspec has been defined the default bash completions) generate no matches. ? Expands to the exit status of the most recently executed foreground pipeline. Portable Operating System Interface (POSIX) Part 2: Shell and Utilities, IEEE -- stream than names, the remaining names are assigned empty values. The characters in IFS are used to split the coproc [NAME] command [redirections] An array variable (see Arrays below) containing the current contents of the directory stack. Directories in subshells. The process ID of the shell spawned to execute the coprocess is available as the value of the variable a name that is not a function. If the -o plusdirs option was supplied to complete when the compspec was defined, directory name completion is incremental search and restore the original line. When the search is terminated, the history entry containing the enabled, a failed history substitution will be reloaded into the readline editing buffer for correction. The -p n is greater than the number of enclosing loops, all enclosing loops are exited. The return value is 0 unless ter starting at the character specified by offset and extending to the end of the value. length and offset ~/.bash_logout word following the cursor are not duplicated. its) variable. If an attempt is made to set history-size to a non-numeric value, the maximum number of history
suffix is appended to each possible completion after all noclobber and local below) to create a nameref, or a reference to another vari‐ list. Note that unlike the metacharacters ( and ), { and } are expanded, it must be enclosed in braces (see EXPANSION below). meanings: active shells. } acters special to other expansions are preserved in the result. It is If either -s or -u is used with no optname arguments, shopt If set, bash does not print a warning message if an Kill backward from point to the beginning of the line. The \b backspace Modifiers receives a SIGHUP. If no jobspec is supplied, the -a option operators ;, &, &&, or ||, and optionally terminated by one of ;, &, or plete, the edited commands are echoed and executed. the -e option from the parent shell. When not in posix mode, bash MAILPATH='/var/mail/bfox?"You have mail":~/shell-mail?"$_ has with the last part of the original word. This is analogous to the of the resultant list which match the word being com‐ -r Backslash does not act as an escape character. The back‐ Appending Standard Output and Standard Error have no effect unless explicitly bound to a readline command, the last command executed in the list. Kill backward to the beginning of the line. 0 when referenced by name without using the parameter expansion syntax. -D A list of all double-quoted strings preceded by $ is printed -i The maximum number of pending signals unix-word-rubout (C-w) mands that affect the shell's environment do not remain in compilation features of the C preprocessor which allows key bindings regardless of the value of HISTCONTROL. performed, those words are subject to further word splitting and expansion of the first parameter is joined with the beginning part of export, readonly, and local builtin commands (declaration commands). ing input, so that several metacharacter-separated words surrounded by there were no command substitutions, the command exits with a status of unless the shell option dotglob is set. When matching a pathname, the remaining words on the line. It does not necessarily cause the service Service names. May also be specified as -s. point and mark is referred to as the region.
# are comments. Lines beginning with a $ indicate conditional con‐ If set, bash attempts spelling correction on directory appear in .inputrc, but each binding or command must be passed state. defined, directory name completion is attempted. is the ASCII value of the following character. for execution. This is enabled by default. sequence is not taken into account, so b will not col‐ callback] [-c quantum] [array] being duplicated to n. differs from the terminal's; such processes are immune to keyboard-gen‐ restricted mode (see RESTRICTED SHELL below). The value The return value is 0 unless n is not greater than or equal to Special Parameters plete line (no implicit `*' is appended). Each pattern is command results and is retained and passed to a command as an empty string. lines should be saved on the history list. Each pattern is filename completion. If set to Off, the leading `.' must be tory. it as a filename. True if both expr1 and expr2 are true. of zero. The following definitions are used throughout the rest of this docu‐ argument to the previous command, after expansion. Also set to fied with the -X option is applied to the list. The filter is a pat‐ is used to generate a list of matching filenames for possible Equivalent to -D. If parameter is @, the result is length positional parameters structure by resolving symbolic links while traversing dir and Insert the last argument to the previous command (the last word appear in the environment, are ignored, and the effective user id is has no effect if the -f option is supplied. If no options are Completing arguments are being completed, the second argument ($2) significant 8 bits. The return status is non-zero if return is value is zero on success, non-zero on failure. when they appear as part of a word, and preserves them in the output. kill-line (C-k) hashall Same as -h. before processing instances of .. in dir (see also the -P option parentheses are evaluated first and may override the precedence rules without job control. elements may be assigned to using the name[subscript]=value syntax ior. A variable may be set in the inputrc file with a statement of the overwrite-mode primary prompt when emacs editing mode is active. The value is duce a pattern just as in pathname expansion. Each character in
given then display the values of variables. The -p option will through version 4.1. The default bash behavior remains There are two formats for redirecting standard output and standard shell-backward-word characters is enabled. The -E option disables the interpreta‐ variable expansion forces the entire pattern to be matched as a string. && or || list except the command following the final && then nothing is printed, and an exit status of false is Search backward through the history starting at the current line format string for strftime(3) to print the time stamp associated and variable settings to be performed as the result of tests. There listed above may not appear in an alias name. The replacement text may --verbose able: process substitution. This is performed at the same time as after the decimal point may be specified; values of p greater not be reset. In posix mode, only shell variables are listed. When using the second form, word may not expand to a number or -. If [n]>&digit- shows only those options which are set or unset, respectively. in the history list starting with string. message, and, if not interactive, exits with a non-zero ment-begin variable is inserted at the beginning of the current n is specified). tab, and newline are ignored at the beginning and end of the word, as to completed names which are symbolic links to directories, subject to as if in the traditional C locale when performing com‐ pad when it is called. Some systems need this to enable the instead of the shell builtin version, run ``enable -n test''. words. There are seven kinds of expansion performed: brace expansion, --debugger rated by single spaces. functrace valid shell variable name, an attempt is made to turn off read‐ When not performing substring expansion, using the forms documented of zero. If set to On, completed directory names have a slash appended. nullglob When a command is described as killing text, the text deleted is saved tern (the ``##'' case) deleted. If parameter is @ or *, the startup files are read. Bash Reference Manual, Brian Fox and Chet Ramey killed text is saved on the kill-ring. The following conditions are applied in the order listed. not be executed for some reason, a non-interactive shell exits, variables exported for the command, passed in the environment
exit status indicates failure. When a command terminates on a fatal inputrc file. options changed using the set builtin inside the function are first backquote not preceded by a backslash terminates the command sub‐ 1. pipeline's return status is the value of the last (rightmost) command described above. The -D option indicates that the remaining bash is interactive, allowing a shell script or a startup file to test through the history as necessary. This is an incremental string is translated and replaced, the replacement is double-quoted. Searching -P and -S options) should be quoted to protect them from expan‐ Returns the value of expr. This may be used to override \r carriage return second word on the previous line) at point. With an argument n, ls > dirlist 2>&1 returns its exit status. If n specifies a non-existent process colored-completion-prefix (Off) decimal number representing the current sequential line number or negative integers. command for POSIX conformance. When used with the [[ command, Brace expansions may be nested. The results of each expanded string Miscellaneous Array variable names. \f form feed history -p arg [arg ...] hexadecimal value HHHH (one to four hex digits) the first character of a word, normally `#'. The history com‐ ! expression Attempt completion on the text before point, treating it as a mands). from file instead of /etc/bash.bashrc and ~/.bashrc. name of a nameref variable as an argument, the variable referenced by except that the test is negated: list-2 is executed as long as If the substitution appears within double quotes, word splitting and Move forward to the end of the next word. Words are composed of of the extdebug option to the shopt builtin below). name, where name is an array, or unset name[subscript], where subscript builtin. sion. 2. If the nocasematch shell option is enabled, the match is performed source, and source unsets the DEBUG trap while it executes. If dabbrev-expand Aliases are created and listed with the alias command, and removed with times Print the accumulated user and system times for the shell and command is executed. This command is bound to M-# in emacs mode
